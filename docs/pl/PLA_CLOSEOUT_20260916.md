# PL-A 광고 축 마감 원장 — 2026-09-16

> P 트랙 광고 축을 닫는다. 어제 「깨짐 521」로 열린 창이 오늘 `[C]=0` 으로 닫혔다.

## 1. 결말 — 광고비가 새는 구멍은 «없었다»

`sa.py verify` 완주(수리된 도구 기준):

| 회차 | 총 | URL없음 | 허브 | 정상 | [A] 적격밖 | [B] 병합 | [C] 깨짐 |
|---|---:|---:|---:|---:|---:|---:|---:|
| relink 전 | 5,520 | 48 | 158 | 4,951 | 202 | **161** | **0** |
| relink 후 | — | — | — | **5,112** | 202 | **0** | **0** |

**튕기는 키워드는 처음부터 0이었다.** 어제의 「DB에 없는 슬러그 521」은 옛 verify 가
「광고 적격 집합에 없으면 깨짐」이라는 «하나의 문» 으로 판정한 오탐이었다.
미분양 집계·공공 계열(적격 밖 정책 분류)과 허브 착지(`/apt/region/…`)가 전부
「깨짐」으로 뭉쳐 있었고, 그 360건에 진짜 결함 161건이 묻혀 있었다.

> 교훈: **오탐이 섞이면 진짜 결함이 안 보인다.** 「깨짐」 하나로 세지 말고 갈라야
> 조치할 수 있는 수가 나온다. 3분류(허브/[A]/[B]/[C])가 그 일을 했다.

## 2. 집행한 것

| # | 내용 | 결과 |
|---|---|---|
| ① | `verify` 3분류 수리 + 병합 사슬 추적(`resolve_survivor`) | [C] 오탐 제거 |
| ② | `relink --from-csv` 신설 — verify 산출 CSV 를 도구가 그대로 먹는다 | 예행→라이브→재검증 풀 사이클 통과 |
| ③ | 병합 dead → survivor 착지 교체 161키워드 | **[B] 161 → 0** |
| ④ | 적격 SQL 의 `ad_blocked` 누락 한 줄 | 차단 현장 11곳이 등록 집합에서 빠짐 |

②의 설계 포인트 둘(다음에 같은 도구를 만들 때 그대로 쓸 것):
- **열 이름을 생산자와 «같은 것» 으로 읽는다**(`old,new,keywords,new_eligible`, utf-8-sig).
  갈리면 조용히 0쌍이 된다 — 실패가 «아무 일도 안 일어난 것» 처럼 보이는 가장 나쁜 형태다.
- ⛔ `new_eligible=N` 은 **건너뛴다**. 그대로 넣으면 사전 검사가 회전 전체를 죽인다 —
  한 쌍 때문에 160쌍이 날아가는 형태(반복 결함형 ⑤). 건너뛴 쌍은 «사유와 함께» 찍는다.

## 3. 남은 칸 — 광고 «재개» 시점의 일 (지금은 닫힌 상태)

이 캠페인은 9/11부터 PAUSED다. 아래는 «다시 켜는 날» 의 목록이지 지금의 숙제가 아니다.

- **트라비스 전용 3그룹 중복 정리** — 트라비스 하늘채#1(45kw)·엄궁 코오롱(71)·엄코 큐샵(81)이
  모두 PAUSED라 지금은 경합하지 않는다. 다시 켜면 같은 검색어가 여러 그룹에 걸려 계측이 쪼개진다.
  ⚠️ 네이버는 광고주 1곳에 검색어 1개당 광고 1개만 보여 준다 — 그룹을 더 만들면
  효과가 늘지 않고 «성과만 쪼개진다».
- **거제시그니처 전용 그룹 A/B/C 판정** — 셋 중 유일한 공백(기존 그룹 0kw).
  갤러리자이·엘포르타는 이미 전용 그룹이 있어 **신설하지 않는 것을 권고**한다.
- **별칭 2건 확인** — 「창원 데시앙」(시공사에서 추론한 이름) · 「남구/신정 래미안」(확정 전 제안명).
  회전 전에 `sa.py scan` 이나 CV-N 게이트로 확인할 것.
- **갤러리자이 CPC 736원 · 일 1.7만원** — 카더라 캠페인 평균(80~240)의 3~9배다.
  착지와 리드 전환을 대조하기 전에는 증액하지 않는다.

## 4. 미회신으로 남는 2건 (광고 축 밖)

- **라로체 착지 URL** — 확인 안 됨
- **더폴 9/3 수집 정지 사유** — 확인 안 됨

⛔ 「확인했는데 문제없었다」가 아니라 «아직 안 봤다» 다. 닫힌 척하지 않는다.

---

# 부록 — MCP 실패 창 기록 (PGRST002 정지 창 대조 재료)

| 항목 | 값 |
|---|---|
| 발생 | 2026-09-16 **14:13~14:20 KST** (05:13~05:20 UTC) |
| 증상 | Supabase MCP `execute_sql` **4연속 타임아웃** (`Connection terminated due to connection timeout`) |
| 대상 질의 | `count(*) FROM bot_hits` · `GROUP BY bot` · `SELECT reltuples FROM pg_class` |
| 판별 | **표 문제가 아니다** — `pg_class` 조회까지 실패했다. 카탈로그 한 줄이 안 나오면 통로다 |
| 같은 창의 DB 상태 | 직전 스냅샷 기준 연결 24~38 · active 2~8 · long_active 0 · blocked 0 — **DB 무죄** |
| 회복 | 약 8분 뒤 같은 질의가 «그대로» 통과 |
| 같은 날 다른 실패 | `site_uptime_log` 조회 3연속 타임아웃(시각 미기록) |

**축적 목적**: 이 창이 PGRST002 정지 창과 겹치는지 대조한다.
겹치면 「관리 API 통로」가 아니라 «PostgREST 재시작 계열» 이라는 다른 진단이 선다.
⛔ 겹치기 전까지는 단정하지 않는다 — 지금까지의 판정은 「통로 문제, 우회 말고 재시도」이고
그 판정은 오늘도 유효했다(우회 없이 재시도로 회복).
