# PL-A 정합 감사 — 2026-09-15

근거: `ad_keywords` 9/15 스냅샷(06:01 KST, 13캠페인 6,085키워드) · `ad_adgroups` · `ad_stats_daily` 14일 · `apt_sites` · `apt_site_merges` · `presale_candidates`.
⛔ 광고 계정 쓰기 0건. API 호출 0건(DB 스냅샷만 읽음). 쓰기는 전부 아래 Node 몫.

---

## ① verify 3분류 수리 — `sa.py verify`

**「깨짐 521」 중 실제로 튕기는 키워드는 0건이었다.** DB로 나눠 보면 (verify 대상 12그룹 기준):

| 분류 | 키워드 | 슬러그 | 뜻 |
|---|---:|---:|---|
| 허브 착지 `/apt/region/…` | 158 | — | 정상. 옛 verify가 `region/부산`을 슬러그로 읽었다 |
| **[A]** 활성 현장이지만 광고 적격 밖(cs<40 등) | 202 | 35 | 페이지는 뜬다. 적격 집합은 신규 등록 기준일 뿐, 페이지 생사와는 무관하다 |
| **[B]** 병합돼 없어진 슬러그 | 183 → **161** | 19 | 301을 한 번 거쳐 착지한다. relink 대상 |
| **[C]** 비활성·미병합 / DB에 없음 | **0** | 0 | 진짜 깨짐 |

158 + 202 + 183 = 543. 여기서 17:15 relink로 옮긴 22건(오티에르 10 + 가야 12)을 빼면 **521로 정확히 맞는다.**

수리 내용:
- `fetch_slug_index()`: `apt_sites` 전체(`is_active`)와 병합 원장을 읽는다. `resolve_survivor()`는 병합 사슬을 끝까지 따라가고, 사슬이 돌면 None을 돌려준다.
- 허브 판정은 `ad-stats-sync`·`import_csv.py`와 같은 규칙을 쓴다(`/`가 들어 있거나 `unsold·pipeline·busan·search·region`이면 허브).
- [B]가 있으면 `out/relink_map_<날짜>.csv`를 떨군다. 이 파일이 ②의 입력이다.
- 통과 조건은 **[B]=0 그리고 [C]=0**이다. [A]와 URL없음(그룹 기본 URL)은 결함으로 치지 않는다.
- 모의 호출로 9개 경로(정상·허브 2·A·B·순환 B·C 2·URL없음)를 확인했다. `test_name_pool.py`도 전부 통과했다.

### 곁에서 찾은 결함: `ad_blocked`가 적격 SQL에 없었다
`apt_sites.ad_blocked` 컬럼 계약은 「true면 sa-sync가 키워드를 등록하지 않는다」인데, 정작 `sa.py` SQL에는 그 조건이 없었다.
실측하니 **적격 집합에 들어와 있는 ad_blocked 현장이 11곳**이었다(주촌 선지리·청량 덕하리 포함). 아직 등록된 키워드는 0건이다.
`AND NOT coalesce(ad_blocked,false)`를 한 줄 넣었다. 다음 build/apply 전에 막아야 해서 이번 감사에 함께 넣었다.

---

## ② 깨짐 relink 맵 — `docs/pl/PLA_relink_map_20260915.csv`

19쌍 · 183키워드. 가야·오티에르 두 쌍은 17:15에 이미 옮겼을 가능성이 높다. 그래도 넣어 두었다(이미 옮겼다면 0건으로 지나간다).
- 새 슬러그 19개 전부 활성이고 광고 적격이다. 사슬 병합은 0건이다.
- 대상 그룹은 `A_분양`, `C_정비사업`, `부울경_E_정비사업_1`이다. 셋 다 relink가 훑는 범위(EXISTING_GROUPS) 안에 있다.
- ⚠️ 이 캠페인(`cmp-…002673`)은 **9/11부터 노출이 0이다**(마지막 노출 9/10, 그룹 전부 PAUSED). 그래서 relink를 해도 당장 지출에는 영향이 없다. **다시 켜기 전에 먼저 해 둬야 하는 작업**이다.
- 록아웃 창은 현재 없다(`LOCKOUTS` 비어 있음).

**Node 원라이너** (예행 → 확인 → `--live` → verify):

```bash
# Git Bash
cd ~/Documents/kadeora/tools/naver-sa && python sa.py relink $(tail -n +2 ../../docs/pl/PLA_relink_map_20260915.csv | tr -d '\r' | awk -F, '{printf "--map %s=%s ", $1, $2}')
#   → 목록이 맞으면 같은 줄 끝에 --live · 이어서 python sa.py verify ([B] 0 확인)
```

```powershell
# PowerShell
cd ~\Documents\kadeora\tools\naver-sa; python sa.py relink @(Import-Csv ..\..\docs\pl\PLA_relink_map_20260915.csv -Encoding UTF8 | ForEach-Object { '--map'; "$($_.old)=$($_.new)" })
```

---

## ③ sa-sync 회전과 신규 13곳 별칭 등록 여부

### 회전 로그는 없다. 회전 자체가 없었다.
계정 키워드 수의 흐름은 `5,990(9/1) → 6,007(9/3) → 6,030(9/8) → 6,085(9/9) → 6,085(9/15)`다.
9/3·9/8·9/9에 늘어난 17·23·55건은 모두 단건 등록(`pl_add_site_keywords` 규모)이다. `sa.py build/apply` 규모의 회전은 **9/6 이후 한 번도 돌지 않았다**.
`sa-sync`는 크론이 아니라 사람이 `sa.py`를 실행하는 절차다. 그러니 「다음 회전에서 등록되는지 기다린다」는 **스스로 오지 않는다.** 누군가 실행해야 한다.

### BP70 신규 13곳 (seeded 12 + 내덕 1)

| 현장 | 슬러그 | 별칭 | 적격 | ad_blocked | 계정 키워드 |
|---|---|---:|:-:|:-:|---:|
| SKY.V 센텀 | skyv-센텀 | 6 | ✓ | | 0 |
| 부곡동 223-1 주상복합 | 부곡동-223-1-주상복합 | 8 | ✓ | | 0 |
| 구포강변뷰 지주택 | 구포강변뷰-지역주택조합 | 4 | ✓ | | 0 |
| 울산 남구 B-04 | 울산-남구-b-04-재개발 | 10 | ✓ | | 0 |
| 울산 더파크 비스타동원 | 울산-더파크-비스타동원 | 9 | ✓ | | 0 |
| 울산 달동 더리브 | 울산-달동-더리브 | 7 | ✓ | | 0 |
| 청량 덕하리 | 울산-청량읍-덕하리-공동주택 | 8 | ~~✓~~ → ✗ | **Y** | 0 |
| 양덕3구역 | 양덕3구역-재개발 | 5 | ✓ | | 0 |
| 경화구역 | 경화구역-재개발 | 5 | ✓ | | 0 |
| 창원 복합행정타운 | 창원-자족형-복합행정타운-공동주택 | 5 | ✓ | | 0 |
| 김해 부원동 지주택 | 김해-부원동-지역주택조합 | 4 | ✓ | | 0 |
| 김해 주촌 선지리 | 김해-주촌면-선지리-공동주택 | 4 | ~~✓~~ → ✗ | **Y** | 0 |
| 김해 내덕지구 1B 2-2L | — (병합 검토 큐 queued) | — | — | — | 0 |

- **DB 쪽 별칭 적재는 12곳 모두 끝났다(4~10개). 계정 등록은 0곳이다.** 회전을 돌리면 10곳이 들어가고, 덕하리·주촌은 ① 수리로 빠진다.
- 표에서 ~~✓~~ → ✗ 는 수리 전에는 적격으로 잡혔지만 ① 수리 후 제외된다는 뜻이다.
- ⚠️ 등록 전 판정이 필요한 별칭 2건:
  - 복합행정타운 「창원 데시앙」: 원문에 브랜드가 없고 시공사 태영건설에서 추론한 이름이다.
  - B-04 「남구 래미안」·「신정 래미안」: 확정 전 제안명이다.
  - 회전 전에 `sa.py scan`이나 CV-N 게이트로 확인할 것.
- **확인 방법:** 회전 다음 날 `ad_keywords` 스냅샷에서 `site_slug in (위 10)`의 행 수가 0보다 큰지 본다. 결과가 좋다는 긍정형 확인이다.

### 트라비스 전용 그룹 — **있다**
| 그룹 | 캠페인 | 키워드 | 입찰 | 상태 | 착지 | 14일 |
|---|---|---:|---:|---|---|---|
| 트라비스 하늘채_광고그룹#1 | cmp-…10823319 | 45 | 550(키워드 최대 8,280) | PAUSED | 그룹 기본 URL(스냅샷에 없음) | 노출 22 · 클릭 0 |
| 엄궁 코오롱 하늘채 | cmp-…10844419 | 71 | 550 | PAUSED | 그룹 기본 | — |
| 엄코 큐샵 | cmp-…10939943 | 81 | 550 | PAUSED | 그룹 기본 | — |

카더라 캠페인 안의 `카더라`(10)·`부울경_D_입주예정`(11) 그룹에도 트라비스 키워드가 있다. ② 맵의 엄궁1 12건도 트라비스로 옮겨 간다.
세 전용 그룹은 모두 PAUSED라 **지금 서로 경합하지는 않는다.** 다만 다시 켤 때 같은 검색어가 여러 그룹에 걸려 계측이 쪼개진다(README export_off 「중복 40」과 같은 형태).

---

## ④ T1 임박 3현장 — 전용 키워드 그룹 신설안 (Node 판정용)

**먼저 확인할 사실:** 셋 중 둘은 **이미 전용 그룹이 있고, 지금 돈이 나가고 있다.**

| 현장 | 카더라 슬러그 | 기존 전용 그룹 | 14일 실적 | 카더라 캠페인 내 |
|---|---|---|---|---|
| 갤러리자이 | 연제-갤러리-자이 | 「연산동 자이갤러리」 ELIGIBLE · 29kw · 착지 그룹 기본 | 클릭 163 · **120,038원** · CPC 736 · 일 ~17,500원 | A_분양 10kw (PAUSED) |
| 엘포르타 | 더샵-아시아드-엘포르타 | 「더샵 아시아드 엘포르타」 ELIGIBLE · 36kw · 착지 그룹 기본 | 클릭 31 · 4,854원 · CPC 157 | A_분양 10kw (PAUSED) |
| 거제시그니처 | 거제-옥포-공동주택 (표시명 「힐스테이트 거제시그니처 — …」 · 1,963세대 · 민영) | **없음** | — | **0kw** |

⚠️ 전용 그룹 둘은 착지가 「그룹 기본 URL」이라 스냅샷에 슬러그가 안 남는다. 카더라 페이지로 가는지 외부(대행) 페이지로 가는지 DB로는 확인할 수 없다. 광고주센터에서 한 번 보는 것이 이 판정의 전제다.
네이버는 광고주 1곳에 검색어 1개당 광고 1개만 보여 준다. 그래서 같은 검색어로 그룹을 **하나 더** 만들면 효과는 늘지 않고 성과만 쪼개진다.

### 거제시그니처 — 신설 필요 (유일한 공백)
그룹 `T1_거제시그니처`(카더라 캠페인), 착지 `/apt/거제-옥포-공동주택`.

| 층 | 키워드(공백 제거) | 입찰 |
|---|---|---:|
| 브랜드 | 힐스테이트거제시그니처 · 거제시그니처 × {"" · 분양가 · 모델하우스 · 청약 · 분양일정 · 견본주택} | 300 |
| 부지 | 거제옥포공동주택 · 옥포공동주택 · 옥포신축아파트 | 100 |

- ⛔ 제외 키워드 2종:
  - `힐스테이트거제`: 기존 기축 단지(`힐스테이트거제`, D_기축 6kw)와 겹친다. 조각이다.
  - `거제아파트분양`·`거제분양`: 이미 E_대표에 있어 중복이다.
- 규모는 약 15키워드다. 등록 경로는 `urgent_keywords.json` + `pl_add_site_keywords.py`와 같은 문(조각·법인명·slug형 판정)을 지나야 한다. 단, 그룹 인자가 A_분양에 고정돼 있어 **그룹 지정 인자를 추가해야 한다**(미구현).
- ⚠️ 이 캠페인은 9/11부터 PAUSED다. 그룹만 켜서는 노출이 안 나갈 수 있다. 캠페인 상태를 먼저 확인할 것.

| 안 | 내용 | 일 예산 상한 | 10월 추정 |
|---|---|---:|---:|
| **A (권고)** | 브랜드 층만 · 지금 켬 → 공고 주간 상한 증액 | 평시 10,000 / 공고~청약 주 30,000 | ~40~50만원 |
| B | 브랜드+부지 · 공고 D-14부터 켬 | 20,000 | ~30만원(2주) |
| C | 보류 — 착지 글 게이트(dry 5에서 거제시그니처 글 차단 이력) 통과 후 | — | 0 |

추정 근거는 실측 벤치 두 곳이다. 분양예정 전용 그룹 기준으로 엘포르타 일 ~600원(CPC 157), 갤러리자이 일 ~17,500원(CPC 736). 거제는 대단지지만 지방 소도시 검색량이라 그 사이로 잡았다. **상한은 소진 목표가 아니라 사고 방지선**이다.

### 엘포르타 · 갤러리자이 — 신설하지 않는 것을 권고
- 기존 전용 그룹의 착지가 카더라 페이지라면 그룹을 새로 만들 이유가 없다. 이 경우 카더라 캠페인 A_분양의 중복 10kw씩은 **다시 켤 때 OFF**한다(열거 CSV로 판정).
- 착지가 외부라면 판정 안건이 된다. 「카더라로 옮길지」는 착지 교체 1건으로 끝나고 무손실이다(sa_relink 규약). 그룹 신설보다 이쪽이 품질지수를 보존한다.
- 갤러리자이 CPC 736원 · 일 1.7만원은 카더라 캠페인 평균 CPC(80~240)의 3~9배다. 착지와 리드 전환을 대조하기 전에는 증액하지 않는다.

---

## Node 판정 대기 목록
1. ② relink `--live` 실행(19쌍, 가야·오티에르는 확인만)
2. ③ 회전 실행 여부 + 별칭 2건(창원 데시앙 · 남구/신정 래미안)
3. ④ 거제시그니처 A/B/C 결정 · 캠페인 PAUSED 상태 확인 · 엘포르타·갤러리자이 전용 그룹의 착지 URL 확인
4. 트라비스 전용 3그룹을 다시 켤지 여부(켜려면 먼저 중복을 정리해야 한다)
