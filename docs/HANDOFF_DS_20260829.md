# 인계 — DS/U-1층 트랙(세션 B) 앞 판정 9건 (2026-08-29)

> **왜 이 파일이 있는가**: 아래 판정들이 2026-08-29에 **세션 A(PV 트랙) 창으로 다섯 번 연달아 전달됐다.**
> 세션 A 는 이 항목들(D-1~D-6 · 뱃지 · CHART_RATIO · 타임라인 · 레일 · 6폭×3모드 · `.touch-target`)을
> 하나도 작업하지 않았고, 그래서 **세션 B 는 승인·기각을 받지 못한 채 멈춰 있었다.**
> 채팅 창은 갈렸어도 저장소는 하나다 — 그래서 여기 옮겨 둔다.
>
> **세션 식별** — 세션 B = `session_019iFeRmYUnjDWKYGsqdgKKp` (`feat(ds)` 커밋을 내는 창) ·
> 세션 A = `session_01JUe8F64ST16JvfExeQchg1` (`feat(pv)` · `feat(u3)`).
>
> ⚠️ 아래는 **본부장 판정 원문의 요지**다. 세션 A 가 해석·가공하지 않았다.
> 충돌하거나 애매하면 채팅 원문이 우선이고, 세션 B 가 본부장에게 재확인할 것.

---

## 판정 9건

| # | 항목 | 판정 |
|---|---|---|
| ① | **3차분** | **승인** — 수치별 뱃지의 「만들면 거짓」 판정과 **블록 단위 + 범위 문구** 처리가 정답 |
| ② | **D-3 프리페치** | **허용** — §6 문구를 「데이터 fetch 0 + 라우트 프리페치 정상」으로 정정. **D 안건에서 제외** |
| ③ | **D-4** | **기각·종결** — 필드별 등급은 **PV-5 산출물**이다. **중단점 B 로 이월**, 현행 유지 |
| ④ | **4차분** | **승인** — 라벨(아는 것)/면책(모를 때 안전한 쪽) 분리 논리 · 「면책은 한 방향으로만 위험」 · 항공 이미지 어휘 통일 · `CHART_RATIO` 공유 수리 전부 옳음 |
| ⑤ | **D-5** | **승인**하되 **I-2 로 편입** — 어드민 이미지 데스크에서 `kind` 입력과 한 몸. **D 안건에서 제외** |
| ⑥ | **D-6** | **즉시 승인** — 서버 선판정으로 **자리 자체를 안 그린다**(빈 섹션 규약 + CLS 동시 만족). 기존 fetch 결과로 조건 렌더(**Rule #49 준수, 추가 fetch 0**). **잔여 작업에 포함** |
| ⑦ | **5차분** | **승인** — 레일 자가 회귀(「재검증 지시가 없었으면 못 찾았다」) · 타임라인 어휘 통합. **「어휘 두 벌이면 한쪽만 늘어난다」를 실례로 STATUS 에 기록할 것** |
| ⑧ | **D-1 (전역 36→44)** | **기각·종결** — 시각 44 전역은 **42px 행 체계와 충돌**한다. 히트 영역 44 는 `.touch-target` 이 **이미 보장**한다. **이원 체계를 표준으로 확정**하고, audit 은 **현행 기준으로 최종 측정** |
| ⑨ | **중단점 D 안건** | **D-2 단독** (기준일 전용 컬럼) — 나머지는 위에서 전부 종결됐다 |

## 이후 순서 (본부장 지정)

```
§5 하드코딩 회수  →  배포  →  §7 전종 게이트  →  6폭×3모드 목록 = 중단점 D 리뷰 요청
```

잔여 작업 목록: 타임라인 · 레일 sticky · **D-6** · §5 회수.
(앞선 전달분에 있던 「고정 높이」·「§9 라벨」·「KeyMetrics 뱃지」는 3~5차분 승인에 흡수된 것으로 읽힌다 — 세션 B 가 자기 진행 상태와 대사할 것.)

---

## 세션 A 쪽에서 넘기는 사실 2건 (D-4·PV-5 접점)

1. **`confidence` 어휘가 5값으로 «양쪽» 잠겼다** (2026-08-29, `pv3b_confidence_vocab_5`).
   `apt_permits.match_confidence` 는 4값이었고 `apt_sites.confidence` 는 **제약이 아예 없었다** —
   PV-1 주석이 「같은 4단계를 쓴다」고 선언했지만 지켜지지 않았다. 이제 둘 다
   `rumor · estimated · confirmed · verified · conflicting` 이다. 뱃지 5값 렌더가 DB 와 일치한다.

2. ⛔ **출처 라벨을 「국토교통부 건축HUB」로 «고정하지 말 것».**
   `apt_sites_expected_sale_source_chk` 는 이미 5값으로 잠겨 있고 라벨 매핑과 1:1 이다:
   `permit`→건축HUB · `news`→언론 보도 · `builder`→시공사 · `announcement`→모집공고 · `admin`→자체 확인.
   현 시드 2건(그랑라크·문수로)은 **`news`** 라, 고정 라벨이면 **첫 화면부터 거짓**이 된다.
   §7-1 은 값별 매핑으로 이행하는 쪽이 정확하다.

---

## 집행 지시 (2026-08-29 · 본부장 → 세션 B)

> 이 지시도 세션 A 창으로 왔다(여섯 번째). 원문 요지를 그대로 옮긴다.

**이 파일의 판정 9건이 전부다 — 채팅으로 재전달하지 않는다.**
파일 머리의 우선순위 규칙대로 집행하고, **해석이 갈리면 임의 판단하지 말고 다음 리뷰 안건으로 적립**할 것.

### 고정 사항 3

1. **판정 ⑦의 STATUS 기록은 세션 B 몫이다.**
   「어휘 두 벌이면 한쪽만 늘어난다」는 세션 B 가 실측한 일이니 **자기 타임라인 어휘로 직접** 기록할 것.
   ⚠️ 참고 — 세션 A 도 같은 날 **같은 형태**를 겪었다:
   `confidence` 가 제약 **4값 / 무잠금** 두 벌이었고 → **잠긴 쪽은 그대로인데 안 잠긴 쪽만 증식** →
   `conflicting` 이 생겼다(`pv3b_confidence_vocab_5_2026-08-29.sql`).
   **같은 병이라는 사실을 STATUS 에 함께 묶을 것.**

2. **44px 미만 15개를 `min-height` 로 올려서 «지우지 말 것».**
   판정 ⑧의 **이원 체계**(시각 = 42px 행 체계 / 히트 = 44px `.touch-target`) 기준으로
   audit 을 **현행 기준 재측정**하고 **결과만 보고**한다.

3. **집행 후 순서는 판정 ⑨ 그대로**:
   `§5 하드코딩 회수 → 배포 → §7 전종 게이트 → 6폭×3모드 목록 제출 = 다음 D 리뷰 요청`
   **다음 리뷰 안건은 D-2 단독.**

---

## 신규 지시 인계 — U-1b 상세(E안) + DS 지역 셀렉 (2026-08-30 · 일곱 번째 오배송)

> 본부장 → 세션 B. 원문 요지를 그대로 옮긴다. **세션 A 는 집행하지 않는다.**

**착수 조건**: 중단점 D 판정 집행 · §7 게이트 · D 리뷰 요청 제출 «완료 후». D-2 판정 대기와는 **병행**.
**스펙 원본** = `docs/디자인시안_v3_지역셀렉_상세E.html` — 두 탭(지역 셀렉 / 상세 E)이 기준이고
**이 파일이 채팅보다 우선**한다. 파일 안 수치는 예시 데이터이니 **레이아웃·토큰·색 규칙만** 취하고 값은 실데이터 바인딩.

### ✅ 선행 blocker 해소 (2026-08-30)
`docs/디자인시안_v3_지역셀렉_상세E.html` **저장 완료**(34,048B).
무결성 확인: `<!DOCTYPE` 시작 · `</html>` 종료 · `<html>`/`</body>` 각 1 · UTF-8 깨짐 0 ·
두 탭(지역셀렉·상세 E) 문자열 확인 · `<style>` 1 · `<script>` 1.
⚠️ 파일 안 수치는 **예시 데이터**다 — 레이아웃·토큰·색 규칙만 취하고 값은 실데이터 바인딩(지시 원문).

### ① U-1b — `/apt/[id]` 를 E안으로 재구성 (U-1a 위에 증분)
- 구조: 히어로(브레드크럼·생애 스트립·지표 4칸) → 일정 → 타입·분양가 → 입지 → 검증 로그 → FAQ → 주변 비교.
  스티키 섹션 내비 + 우측 레일(리드폼 sticky, 900px 미만에서 본문 위 = 폼 상단 제약 유지).
- **제외 확정**: 본부 노트 «없음» · 전화 상담 CTA **전면 금지**.
- **CTA 2종만**: 「관심고객 등록」(기존 리드폼) + 「부동산 정보공유 톡방 입장」(카카오 옐로우, 하단 바 + 레일 카드 2곳).
  ⚠️ 톡방 URL 은 **환경변수** — 값은 Node 가 준다. **없으면 버튼 숨김**.
- **강조색 3역할 고정**: 골드=행동(섹션 틱바·D-day 배지·현재 단계·등록 버튼·청약 지표칸) ·
  네이비=핵심 값(지표·분양가·하이라이트 행) · 검증 3색=뱃지 전용. **이 밖에 색 쓰지 말 것.**
- 데이터 없는 필드·섹션은 **조건부 렌더** — 빈 칸을 예시값으로 채우기 금지(D4 경계).
  FAQ 는 공통 템플릿 2~3문항으로 시작(현장별 커스텀은 추후 안건). 주변 비교는 비교 데이터 있는 현장만.
- 게이트: 3모드(라이트/다크/큰글씨)가 **토큰 오버라이드만으로** 전환 — 컴포넌트 CSS 분기 금지.
  42px 행 / 히트 44 이원 체계 · §7 전종 게이트 · 6폭×3모드 통과.

### ② DS 신규 — 지역 셀렉(검색형), **전국 기준**
- 검색 자동완성이 1차 경로 · 시도 그리드+시군구 칩이 탐색 경로 · 다중 선택 + 하단 적용 바. 시안 1탭 그대로.
- 지역 데이터는 시안 HTML 하드코딩을 복붙하지 «말고» 공유 지역코드 모듈에서 생성 — **진실은 DB/모듈이다.**
- 선택 상태는 **URL 쿼리로 직렬화**(공유 가능 · 목록 SSR 필터 연동), 카운트는 실측값.
- 적용 대상: `/apt` 목록 **우선**. 전역 헤더 편입은 다음 리뷰 안건.

**완료 시 두 건을 묶어 실화면 리뷰 요청 = 중단점 E.** 프로덕션 실측(6폭×3모드·게이트 표) 형식은 D 리뷰와 동일.

### 세션 A 가 붙이는 사실 — ②의 「지역 데이터」 원천 (PV-1 이 만든 모듈이다)

| 모듈 | 커버 | 지역 셀렉에서의 용도 |
|---|---|---|
| `src/lib/region/lawd.ts` `SIGUNGU_LAWD_CODES` | **전국 251 코드 · 17 시도 · 라벨 230** | ✅ **시도 그리드 + 시군구 칩의 원천은 여기다** |
| `src/lib/region/bjdong-data.ts` `BJDONG_BY_SIGUNGU` | **부울경만**(시도 26·31·48 · 43 시군구 · 2,834 법정동) | ⛔ 전국 셀렉에 쓰면 «부울경 밖이 통째로 빈다» |

- 라벨은 `'시도 시군구'` 형태(예: `'부산 해운대구'`), 세종만 단일 토큰(`'세종시'`)이다.
- 라벨 230 < 코드 251 인 이유: 창원처럼 **한 라벨이 여러 구 코드**를 갖는다. 칩 하나 = 코드 여러 개일 수 있으니
  URL 직렬화는 **라벨이 아니라 코드 배열**로 하는 편이 안전하다.
- PV-1 이 주석에 남긴 결함(안산 부재·창원 부분·`수원영통`/`청원구` 오라벨)은 2026-08-30 재확인 결과
  **라벨 축에서는 해소돼 보인다**(안산·창원·수원 정상, 청원 없음). 코드 축까지 검증하진 않았다.
- ⚠️ 「카운트는 실측값」 요구 — 시군구별 현장 수는 `apt_sites`(6,033행)에서 세면 되지만,
  `region`·`sigungu` 는 **문자열 라벨**이라 코드와 조인하려면 `labelOfLawdCode()` 를 거쳐야 한다.

---

# 세션 A 앞 실측 안건 4건 — 지역코드 세대 (2026-08-30 · 세션 B 기록)

> 본부장 지시로 세션 B 가 `lawd-codes.ts` 창원 문제를 **이관받아 수리**하면서 나온 것들.
> 넷 다 **「우리 코드가 어느 세대인가」** 라는 한 질문이고, 답하려면 **소비처 API 실호출 1건**이면 된다.
> 세션 A 의 **B′ 보고에 실측으로 얹어 주기 바란다** — 그 전까지 세션 B 는 손대지 않는다.

## A-1. 건축HUB `sigunguCd` 는 광주(29)·전남(46) 을 아직 받는가 ★

StanReginCd 실측(2026-08-30): 시도가 **15개**이고 **`12 전남광주통합특별시`(3,204행)** 으로 통합됐다.
29·46 은 **0행**(폐지). 그런데 `lawd.ts` 는 아직 29 5곳 · 46 22곳을 쓴다.

- 그래서 `bjdong-data.ts` 전국 생성에서 **광주·전남이 통째로 빠졌다**(생성물 헤더에 「전국 아님」 명기).
- ⛔ `lawd.ts` 를 12 계열로 바꾸지 **않았다** — StanReginCd 가 앞서 있고 건축HUB 가 옛 코드를
  아직 받는다면, 바꾸는 순간 광주·전남 수집이 **통째로 죽는다.**
- **필요한 실측 1건**: 건축HUB `getApBasisOulnInfo` 에 `sigunguCd=29110`(광주 동구) 과
  `12xxx`(통합 코드) 를 각각 넣어 어느 쪽이 응답하는지.

### ✅ A-1 실측 답 (2026-08-30 · 세션 A · 건축HUB 실호출 12발)

**건축HUB 는 이미 통합 코드(12)로 전환됐다. 옛 광주(29)·전남(46) 코드로는 데이터가 오지 않는다.**

`ArchPmsHubService/getApBasisOulnInfo` · 같은 `bjdongCd` 로 시군구 접두만 바꿔 비교:

| sigunguCd | bjdongCd | resultCode | totalCount |
|---|---|---|---|
| `29110` 옛 광주 동구 | 10100·10200·10300 | 00 NORMAL | **0** |
| `29140` 옛 광주 서구 | 10100·10200 | 00 NORMAL | **0** |
| `46110` 옛 전남 목포 | 10100·10200 | 00 NORMAL | **0** |
| **`12110` 통합** | 10100 | 00 NORMAL | **1,227** ✅ |
| `12140` 통합(추정) | 10100 | 00 NORMAL | 0 |
| `26110` 부산 중구 (대조군) | 10100 | 00 NORMAL | **440** ✅ |

**판정: `lawd.ts` 를 12 계열로 «바꿔도 광주·전남 수집이 죽지 않는다» — 오히려 지금이 죽어 있다.**
- 봉투는 셋 다 `00 NORMAL SERVICE` 다. 「거부」가 아니라 «데이터가 없다» 로 온다 —
  ⚠️ 옛 코드를 그대로 두면 이 0 이 「그 지역에 인허가가 없다」로 조용히 집계된다.
  오늘 종일 잡은 그 형태이고, 여기서도 같다.
- 통제 비교가 근거다: **같은 `bjdongCd=10100`** 인데 `12110` 은 1,227, `29110` 은 0 이다.
  법정동 코드 탓이 아니라 «시군구 접두» 탓이다.
- `12140` 0 은 그 코드가 실제 시군구가 아니거나 그 동에 데이터가 없는 것 — **추정 코드였다.**
  전환 시 12 계열 실제 코드는 StanReginCd 에서 받아야 한다(추정 금지).

**세션 B 앞 결론**: 15시도 전환 **진행 가능**. 다만 12 계열 시군구 코드는 StanReginCd 실측본을
쓰고, 전환 후 «시군구별 totalCount 0 인 곳» 을 한 번 훑어 진짜 0 인지 코드 오류인지 가릴 것.
⛔ 전환은 `lawd.ts`(세션 A 자산)를 건드리므로 **세션 A 와 순서를 맞춰야 한다** — 같은 파일을
   두 세션이 동시에 고치면 충돌한다. 세션 B 가 착수 시점을 알려 주면 세션 A 가 비켜 준다.

## A-2. 인천 행정구역 개편 — `apt_sites` 117행이 걸려 있다

StanReginCd 실측: **28110(중구)·28140(동구)·28260(서구) 이 0행**(폐지)이고,
**제물포구 28125 · 영종구 28155 · 서해구 28275 · 검단구 28290** 이 신설돼 있다.

- ⛔ 세션 B 가 **건드리지 않았다.** `apt_sites` 에 `인천 중구 37 · 동구 10 · 서구 70 = 117행`이
  옛 라벨을 쓰고 있어, 라벨을 지우면 그 행들이 고아가 된다.
- 광주·전남과 **같은 성격**이다(데이터 마이그레이션이 딸린 개편) → 같은 규율로 보류.

## A-3. `crawl-apt-resale` 의 «세 번째» 지역표

`src/app/api/cron/crawl-apt-resale/route.ts:42` 에 자체 `LAWD_CODES` 가 또 있다.
세션 A 가 PV-1 에서 「부산진구·수영구를 다른 구 코드로 긁는다」고 적어 둔 항목이라 **범위 밖으로 뒀다.**
⚠️ `lib/lawd-codes.ts` 의 표는 이번에 지웠으므로, 이제 남은 중복은 **이 하나뿐**이다.

## A-4. 전국 수집 호출 예산 — 한도의 3.3배 → **4배로 갱신됨(2026-08-30 승인)**

`bjdong-data.ts` 전국판 실측: **법정동 16,688 × 2트랙 = 33,376/일** · data.go.kr 일 한도 **10,000**.
부울경 시절 5,668 과는 규모가 다르다 — 분할 순회로도 하루에 안 끝난다.
→ 본부장 판정대로 **전국 전수 수집은 이번 범위가 아니다.** 셀렉 UI 는 전국 + 실측 카운트(0 은 0으로 정직 표기)로 가고,
   빈 지역을 채울지는 **수집 확장 결정(Node 몫)** 이다. 이 수치를 그 안건의 근거로 첨부한다.

---

# 세션 B 수리 완료 보고 — `lawd-codes.ts` 이관 (2026-08-30)

본부장 지시로 **이관받아 닫았다**(세션 A 의 「고치지 않았다」 유예를 무단으로 닫은 것이 아니다).

## 무엇이 틀려 있었나 — 둘이다

| | |
|---|---|
| ① 라벨 1개에 코드 **하나** | `'경남 창원시':'48121'` — 의창구만 긁히고 성산·마산합포·마산회원·진해 **4구가 빠졌다** |
| ② **폐지된 코드**를 들고 있었다 | `42xxx`(구 강원) 18 · `45xxx`(구 전북) 14 |

②가 세션 A 가 관측한 **「crawl-apt-rent 이 강원·전북 0행」의 원인**이다 —
없는 코드로 물어보고 0을 받아 왔고, 그 0 은 「거래가 없다」와 구분되지 않았다.

## 계약 검증 (소비처 2곳)

- `parseRegionSigungu` 는 두 모듈에서 **구현이 완전히 동일** — 교체가 동작을 바꾸지 않는다.
- 호출 계약은 `[라벨, 코드]` 쌍으로 동일. `Object.entries` → **`flatMap` 으로 편다**
  (라벨 단위로 세면 창원 5구가 다시 1건이 된다 — `crawl-apt-trade` 선례 그대로).
- **코드 대조**: 죽은 코드 **32개 제거** / 실재 코드 **52개 추가**
  (`51` 강원 18 · `52` 전북 15 · `41` 경기 일반구 11 · `48` 창원 등 4 · 기타 4).

## 부수 공백 — 근거 있는 것만 등재했다

| 처리 | 근거 |
|---|---|
| `경기 화성시` → 4개 구 코드 · `경기 부천시` → 3개 구 코드 | 시 코드(41590·41190)는 StanReginCd 에서 **머리 행 1건뿐** — 법정동이 전부 구 아래에 있어 시 코드로는 0건. 창원과 같은 구조. **라벨 불변이라 DB 영향 0** |
| `경북 군위군`(47720) → `대구 군위군`(27720) | 47720 은 StanReginCd **0행**(2023 대구 이관). `apt_sites` 에 `군위군` **0행**이라 딸린 데이터 없음 |
| 인천 4개 신설 구 | ⛔ **등재하지 않았다** — A-2 참조(117행 딸림) |

⛔ **근거 없는 코드는 하나도 추가하지 않았다.** 이름은 전부 StanReginCd `locatadd_nm` 실측이고,
살아 있는지/죽었는지도 실호출로 확인했다. 등재 자체가 판정이다.

---

# ▶ 세션 B 재시작 인계 (2026-08-30 · 컨텍스트 한계로 세션 교체)

> **새 세션은 이 절 + 아래 「읽을 문서」만으로 착수할 수 있어야 한다.**
> 그게 안 되면 이 절이 부족한 것이니, 먼저 여기를 보강하고 시작할 것.

## 0. 나는 누구인가 · 어디서 일하는가

- **세션 B = DS/U 트랙**(디자인 시스템 · `/apt/[id]` 상세). `feat(ds)` · `feat(u1a)` 커밋을 낸다.
- **작업 저장소**: `C:/Users/82105/Documents/kadeora-ds` (브랜치 `ds`, upstream `origin/main`).
  `git push` 만 치면 `ds → origin/main` 으로 나간다. 커밋 전 `git pull --rebase origin main`.
  ⚠️ **Claude Code 는 `C:\WINDOWS\system32` 에서 띄운다** — 다른 폴더에서 띄우면 기억이 안 보인다.
  ⚠️ 세션 A(PV) 는 `Documents/kadeora`(브랜치 `main`)에서 돈다. **같은 원격을 쓰므로 push 전 rebase 필수.**
- **로컬 한계**: `.env.local` 의 SERVICE_ROLE 은 placeholder 다 → **`/apt/[id]` 가 로컬에서 렌더되지 않는다**
  (데이터 0건 → `/apt/search` 로 리다이렉트). **상세 화면 검증은 배포 후 프로덕션에서만 가능하다.**

## 1. 읽을 문서 (이 순서로)

| 순서 | 문서 | 무엇 |
|---|---|---|
| 1 | `docs/MASTER_20260829.md` | 최상위 기준 · 부록 A~D(회신·대사·판정·B8 검증) |
| 2 | `docs/DS_RULES.md` | **카피·밀도·토큰 헌법 + 게이트 철학 4조.** 코드 쓰기 전에 읽을 것 |
| 3 | `docs/U_20260829.md` §3 | U-1층 전체 설계(U-1b 가 이어받을 범위가 여기 있다) |
| 4 | `docs/U1A_20260829.md` | U-1a 정본(§3 배치표·§5~§9) + 부록 A-1(확신도 어휘) |
| 5 | `docs/DS_20260829.md` | DS 트랙 설계서 · §8 중단점 C 안건(종결분 포함) |
| 6 | 이 파일 전체 | 판정 9건 · 세션 A 앞 안건 4건 · 이 절 |

## 2. 지금까지 (전부 배포 완료)

`B8` 상세 하단 CTA → `DS-1` 토큰 3계층 → `DS-2` 표준 8종 + `/admin/design` →
`DS-2d` 뽀짝 A안(라운드 8/12/16 · 여백 4의 배수) → `DS-3` 4화면 → `DS-4` 규칙 문서 →
`U-1a` 상세 개편(배치표 전 항목) → 지역코드(법정동 전국 · `lawd-codes` 이관 · 별칭).

**게이트 6종** — 커밋 전 해당하는 것을 돌린다:
`scripts/token-snapshot.ts`(6폭×5모드) · `contrast-audit.ts` · `font-large-guard.ts` ·
`touch-spacing-audit.ts` · `ds-6x6.ts`(6폭×3모드×6경로) · `smoke.ts`
⚠️ 발동 조건을 지킬 것 — `.css`/토큰을 안 건드린 커밋에 token-snapshot 을 돌리지 않는다(DS_RULES §4).

## 3. 잔여 — 새 세션이 할 일

### ① U-1b 착수 (주 과제 · 지금 바로 가능)
`docs/U_20260829.md` §3 U-1층 중 **U-1a 가 «범위 밖」으로 남긴 것들**이 U-1b 다.
`docs/U1A_20260829.md` §8 이 그 목록이다:

- **`permits` 이벤트 타임라인 실편입** — 지금 타임라인(`SiteHistoryTimeline`)은
  `apt_site_events` 만 읽는다. `apt_permits` 를 같은 축에 얹는 일.
  ⚠️ 확신도 뱃지는 이미 DS ⑥ `VerifiedBadge` 로 통합돼 있다(5값 + null). 어휘를 새로 만들지 말 것.
- **GEO 요약 블록 + FAQ JSON-LD** — 상단 자기완결 3~4문장 · FAQPage 구조화 데이터.
- **U-2층 구독 CTA 진입점** — 「이 현장 공고 뜨면 알려줘」. U-2 본체는 별건이고 «진입점만».
- **`DetailShell` 퇴역**(Rule #19).

⛔ **착수 전 확인**: 중단점 D 가 아직 «안 열렸다». U-1a 완료 보고(6폭×3모드 + 게이트 전종)는
   냈고 **판정 6건(D-1~D-6)이 미회신** 이다. U-1b 를 시작하되, D 판정이 오면 그쪽이 우선이다.

### ② 15시도 셀렉 전환 — **A-1 대기**
본부장 지시: 「A-1 실측이 HANDOFF 에 실리면 pull 해서 15시도 전환을 마저 진행」.
→ 이 파일의 **A-1** 절에 세션 A 의 답(건축HUB 가 29 를 받는가)이 실렸는지 먼저 확인할 것.
- 답이 «옛 코드도 받는다» → `lawd.ts` 를 12 계열로 옮기고 셀렉을 15시도로. 별칭 표에 광주 5구·전남 22곳 추가.
- 답이 «신 코드만 받는다» → 같음(더 급하다).
- 아직 없으면 **건드리지 말 것.** `bjdong-data.ts` 헤더의 「전국 아님」 표기도 그대로 둔다.
⚠️ 별칭 구조는 이미 있다 — `src/lib/region/alias.ts` 의 `MOVED` 표에 줄만 추가하면 된다.

### ③ 상시 대기 안건
- **중단점 C 잔여**: EmptyState 3종 통합(`components/` · `components/ui/` · `components/ds/`) ·
  C-5 토스 배지 대비 3건(배지 첫 사용 화면에 동봉).
- **`P-write`**: 종결됨(/write 유지). smoke owner 는 `U-1(상세 관련글 진입점)` 로 이관돼 있다.

## 4. 이 세션에서 «일부러 안 한」 것 (되풀이 금지)

| 안 한 것 | 이유 |
|---|---|
| `lawd.ts` 를 12(전남광주통합) 계열로 전환 | 건축HUB 코드 세대 미실측(A-1). 바꾸면 광주·전남 수집이 죽을 수 있다 |
| 인천 중구·동구·서구 라벨 제거 | `apt_sites` **117행**이 그 라벨을 쓴다. 데이터 마이그레이션 딸린 별건(A-2) |
| `crawl-apt-resale` 의 세 번째 지역표 | 세션 A 가 별건으로 명시한 항목(A-3) |
| 전역 `min-height:36` → 44 | 모바일 «전 화면» 이 8px 자란다. D-1 판정 대기 |
| 전국 전수 수집 | 33,376 호출/일 = 한도 3.3배. 본부장 판정: 이번 범위 아님(A-4) |
| 「조감도」 단정 | 조감도/사진 구분 필드가 «없다». 라벨과 면책을 분리했다(D-5) |

## 5. 이 트랙의 판단 규율 (짧게)

1. **게이트가 실패하면 «단언의 전제» 부터 의심한다.**
2. **부대조건은 이행 대상이 아니라 «검증 대상» 이다** — 도구가 못 재면 도구를 먼저 고친다.
3. **숫자를 고치기 전에 «자» 를 고친다.**
4. **데이터 양이 다른 두 화면을 «개수» 로 비교하지 않는다**(로컬은 행이 적게 렌더된다).
5. **라벨이 데이터를 앞지르면 «라벨» 을 고친다** — 합성값으로 정렬을 갈아타면 거짓이 커진다.
6. **모르면 «지어내지 않는다»** — null 을 돌려주고 사유를 주석에 남긴다. 알고 둔 것과 모르고 둔 것은 다르다.


---

# ✅ 중단점 D 집행 완료 보고 = **D 리뷰 요청** (2026-08-30 · 세션 B)

배포 `456508be`·`356a875d`·`296e25e3` 까지 프로덕션 반영 확인.

## 1. 판정 9건 집행 결과
| 판정 | 집행 |
|---|---|
| D-6 (즉시 승인) | 차트 자리를 **서버가** 판정 — `trades.length >= 2`. 추가 fetch 0(Rule #49). 잔여 1건(서버는 nameVariants 로 넓게, 차트 API 는 이름 정확일치+sigungu 로 좁게)은 코드 주석에 남김 |
| ② §6 문구 | 「데이터 fetch 0 + 라우트 프리페치 정상」으로 정정. 스크롤 시 17건은 전부 Next `<Link>` 프리페치, `/api/*` 0건 |
| ⑦ STATUS | 「어휘가 두 벌이면 한쪽만 늘어난다」를 **두 트랙 실례로 묶어** 기록(세션 B 타임라인 3값 · 세션 A confidence 4값/무잠금) |
| ⑧ 이원 체계 | `DS_RULES §1-5` 에 **표준으로 확정**. 전역 `min-height:44` 금지를 규칙에 박음 |
| D-1·D-4 | 기각·종결 — 손대지 않음 |
| D-3·D-5 | 제외 — 손대지 않음 |

## 2. §7 게이트 전종 (프로덕션 실측)
| 게이트 | 결과 |
|---|---|
| `ds-6x6` | **108검사 0실패** — 6폭 × 3모드 × 6페이지, 가로 넘침 0 |
| `contrast-audit` | 12검사 0실패 / 안건 대기 3 (toss 스킨 C-5, 종전과 동일) |
| `font-large-guard` | 6검사 전부 통과 **(자 수리 후)** |
| `touch-spacing-audit` | 아래 §3 — 이원 체계 기준 **측정만** |
| `b8-widths` | 상세 URL 기준 **전부 통과** (5폭) |
| `smoke` | 89검사 0실패 / 예정 경고 3(`/feed`, U-1 몫) |
| `token-snapshot` | 기준선을 저장소에 등재(`docs/gates/token-baseline.txt`) |
| ad-safety 경로 | 직접진입 = 광고파라미터 **완전 동일**(설계대로 리드폼 기준). 전화번호 0 · 「전화 상담」 0 · 「분양 개시/확정 분양가」 0 |
| 리드 폼 | **비파괴 프로브로만 확인** — 아래 §4 |

### 6폭 × 3모드 목록 (ds-6x6)
```
        default    font-small  font-large
 390px  6/6 ✅     6/6 ✅      6/6 ✅
 480px  6/6 ✅     6/6 ✅      6/6 ✅
 700px  6/6 ✅     6/6 ✅      6/6 ✅
 768px  6/6 ✅     6/6 ✅      6/6 ✅
1024px  6/6 ✅     6/6 ✅      6/6 ✅
1280px  6/6 ✅     6/6 ✅      6/6 ✅
```

## 3. 터치 감사 — **현행 이원 체계 기준 최종 측정** (⛔ 아무것도 올리지 않았다)
| 경로 | default | font-small | font-large |
|---|---|---|---|
| `/` | 44px미만 3 · 최소gap 4 | 3 · 4 | 3 · 5 |
| `/apt` | **0** · 6 | **0** · 6 | **0** · 6 |
| `/blog` | 7 · 4 | 7 · 4 | 7 · 6 |
| `/apt/그랑라크` | 15 · 2 | 17 · 2 | 7 · 2 |
| `/apt/대연 푸르지오` | 21 · 2 | 24 · 2 | 13 · 2 |

- 걸린 요소는 대부분 **빵부스러기(홈·부동산·울산)** 로 실측 36px · gap 2px 다.
- ⚠️ **대연 푸르지오는 오늘 처음 쟀다.** 자에 상세가 한 경로뿐이었다(`db4b0c98` 은 2경로라 적었지만 실제로는 1개). 넣자마자 21건이 나왔다 — 늘어난 게 아니라 **안 보고 있었다.**

## 4. ⛔ 하지 못한 것 1건 — 리드 E2E 「전송」
§7 의 「상단 폼 리드 E2E 1건」 중 **전송 단계는 실행하지 않았다.** 실제 리드가
외부 시트에 기록되는 쓰기라 자동 승인에서 차단됐다.
대신 **쓰지 않고 확인되는 절반**을 재서 게이트로 등재했다(`scripts/lead-form-probe.ts`):
- 폼 렌더 정상(입력 7칸 · 허니팟 1 · 동의 2)
- 빈 폼 제출 시 검증이 **막고**, 그 동안 리드 엔드포인트로 **전송 0건**
- 🔧 그 과정에서 결함 1건 수리 — 에러 문구는 4개 뜨는데 `aria-invalid`·`role="alert"` 가 **0개**였다(DS_RULES §2-6 위반). 배선 후 재측정 **8개** 확인.

→ 전송까지 필요하면 지시 바람. `! npx tsx scripts/lead-form-probe.ts` 옆에
   전송판을 두는 것도 가능하나, **테스트 리드가 담당자 시트에 남는다**.

## 5. 새로 나온 안건 (등재만, 착수 안 함)
| # | 내용 |
|---|---|
| D-a | `ad-safety.isConfirmed()` 가 **`'confirmed'` 한 값만** 확정으로 친다. 5값 체계에서 가장 강한 `verified` 가 «미확정» 으로 접힌다 — 방향은 안전(숨김) 쪽이지만 「확정 표기는 verified」 정책과 어긋난다. 어휘 두 벌의 **세 번째** 사례 |
| D-b | 문자열 단축 여백 ~200건(`padding: '5px 6px'` 계열). 사다리 «밖» 이 아니라 «아래» 라 기계적 반올림 금지 — 자리별로 조밀 표 여부를 갈라야 한다 |
| D-c | `marginTop/Bottom` 숫자 리터럴은 이번 회수에서 **제외**했다(여백 축이 아니라 리듬 축이라 별도 판단 필요) |

## 6. 다음 안건 = **D-2 단독** (지시대로)


---

# 📣 착수 통보 — ② 15시도 전환 (세션 B, 2026-08-30)

**`src/lib/region/lawd.ts` 를 지금부터 건드린다.** 세션 A 는 이 파일을 비켜 주기 바란다
(A-1 회신의 요청 그대로). 끝나면 이 절에 완료를 적는다.

## 실측 먼저 — 추정 코드는 넣지 않는다
`npx tsx scripts/stanregin-sigungu.ts 12` (신설, 읽기 전용):

```
시도 12 — 전체 3,204행 · 시군구 머리행 27개
  12110 목포시   12130 여수시   12150 순천시   12170 나주시   12190 광양시
  12210 동구     12240 서구     12270 남구     12300 북구     12330 광산구
  12710 담양군 … 12870 신안군  (군 17)
```
- **27 = 옛 광주 5구 + 옛 전남 22곳.** 수가 맞는다.
- ⚠️ A-1 이 시험한 `12140` 은 이 목록에 **없다**(추정 코드였다). 실측본만 쓴다.

## 이 전환이 «판단» 을 하나 요구한다 — 시도 표시 라벨
StanReginCd 의 시도명은 「전남광주통합특별시」다. lawd.ts 의 축약 관례
(강원특별자치도→`강원` · 전북특별자치도→`전북`)를 따르면 **`전남광주`** 다.
→ 라벨은 `전남광주 동구` · `전남광주 목포시` 형태가 되고,
  `광주 동구`·`전남 목포시` 는 **alias(MOVED) 로 들어오는 말**만 받는다.
⚠️ 이 라벨은 URL·DB 조인(`apt_sites.region`/`sigungu` 문자열)에 닿는다 —
   조인 영향 확인을 전환의 «첫 단계» 로 둔다. 라벨만 바꾸고 조인이 깨지면
   광주·전남 현장이 화면에서 통째로 사라진다.


## ✅ ② 15시도 전환 — 완료 (2026-08-30). 세션 A 는 `lawd.ts` 를 다시 써도 된다.

### 판정 (본부장) — **코드만 12로, 라벨은 유지**
라벨은 `parseRegionSigungu` 를 거쳐 DB 의 `region`/`sigungu` 어휘가 된다.
`apt_sites` 에 그 어휘로 **480행**(전남 361 · 광주 119)이 이미 있다 —
라벨까지 바꾸면 새로 쌓이는 행만 새 어휘가 되고 옛 480행은 잠긴 채 남는다.
「어휘가 두 벌이면 한쪽만 늘어난다」를 우리 손으로 재현하는 일이라 하지 않았다.

### 한 것
| | |
|---|---|
| `lawd.ts` | 29·46 → **12 계열 27개**. 실측본만 썼다(`scripts/stanregin-sigungu.ts 12`) |
| 테스트 | 「29·46 이 다시 들어오면 실패」 + 「12 계열은 27개」 + 시도 접두 12 잠금 |
| `alias.ts` | **MOVED 에 줄을 넣지 않았다** — 라벨이 안 바뀌었으니 「옛 이름 → 현재 라벨」이 «같은 말» 이 된다. 아무 일도 안 하는 27줄은 다음 사람을 오도한다. 사유를 `MERGE_LABEL_NOTE` 로 남겼다 |
| `bjdong-data.ts` | 재생성 — 시군구 **217 → 252** · 법정동 **16,688 → 20,196**. 「이 표는 전국이 아니다」 헤더 **사라짐**(0행 시도 없음) |

### ⚠️ 보고할 사실 셋
1. **「15시도」가 아니라 «16시도» 다.** 실측 요청 목록:
   `11·12·26·27·28·30·31·36·41·43·44·47·48·50·51·52` — 17개에서 광주·전남이 하나로 합쳐 16이다.
   셀렉 UI 는 이 16을 기준으로 만든다.
2. **인천 4코드가 지역표에 없다** — `28125·28155·28275·28290`. StanReginCd 에는 있는데
   `lawd.ts` 가 모르는 시군구라 법정동이 통째로 버려진다. **A-2(인천 개편, apt_sites 117행)** 그 건이다.
   세션 A 안건이라 손대지 않았다.
3. 호출 예산이 다시 커졌다 — `20,196 코드 × 2트랙 = 40,392` / 일 한도 10,000(**4배**).
   A-4 안건의 33,376 이 이 값으로 갱신된다. 전수 수집은 여전히 범위 밖이다.


---

# ✅ D-2 종결 — 분양예정시기 기준일 (2026-08-30)

## 재사용 1안을 «먼저 재고» 못 닿는 것을 확인했다
| 확인 | 실측 |
|---|---|
| `apt_fact_checks` 가 검증하는 필드 | `display_name` · `total_units` · `builder` **셋뿐** (78행) |
| `expected_sale_period` 행 | **0건** — 없는 행의 `checked_at` 은 기준일이 될 수 없다 |
| 화면 쿼리 예산 | 2폭 파도 고정(Rule #49). 오늘 0건인 표를 읽자고 규율을 깰 수 없다 |
| 진짜 기준일 | **이미 있다 — 다만 `confidence_note` 안 «산문»** (「뉴시스 2026-08-10」·「헤럴드경제 2026-08-24」) |

⛔ 문장에서 날짜를 파싱해 표시광고 근거로 삼지 않았다. 형식이 보장되지 않는다.

## 한 것
- `apt_sites.expected_sale_period_asof date` 신설 + `asof` 는 `period` 있을 때만 허용하는 제약.
  **역방향 제약은 걸지 않았다** — 인허가 승격 경로가 아직 기준일을 안 실어 보내므로
  지금 걸면 그 크론이 조용히 실패한다(크론은 항상 200 이라 더 안 보인다).
- 백필 2건 — `2026-08-10` · `2026-08-24`. 근거는 각 행 노트가 **인용한 보도일**이다.
- `salePeriodDisplay()` 신설(Rule #116) — **4요소가 다 모일 때만** 표시. 5검사 추가.
  ⛔ 부분 표시 금지: 출처·등급만 붙은 시기는 「검증된 일정」으로 읽히는데
     기준일이 없으면 «언제 기준인지 모르는» 검증이 된다.
- 히어로 렌더를 그 판정에 물렸다. 기준일이 없는 현장은 **줄 자체가 안 나온다.**

## ⚠️ 기준일의 «집은 하나» 다
나중에 verify-facts 가 이 필드를 검증하게 되면 그 as-of 도 **같은 컬럼에** 쓴다.
두 곳에 적으면 한쪽만 늘어난다 — 오늘 STATUS 에 세운 공리 그대로다.

## A-4 예산 갱신 (승인 반영)
`33,376/일` → **`40,392/일`** (20,196 코드 × 2트랙 · 한도 10,000 의 **4배**).
결론 불변 — 전수 수집은 범위 밖. 인천 4코드는 **A-2 소관 유지, 손대지 않았다.**


---

# ✅ 리드 E2E 1건 — 도달 확인 (2026-08-30 · Node 승인건)

`npx tsx scripts/lead-e2e-once.ts https://kadeora.app` · 그랑라크 상세 · 390px 모바일.

| 확인 항목 | 결과 |
|---|---|
| 폼 배너(분기 표시) | 「**진행 상황 알림 · 무료**」 — pre_notice 문구가 떴다 |
| 전송 | Apps Script 302(리다이렉트) → 화면 「신청이 접수되었습니다」 |
| **DB 도착** | `leads` 신규 1행 — `7ddc081b-9596-4fc4-8478-940414059e22` · `2026-08-30 01:02:18Z` (KST 10:02) |
| 표식 규약 | `name = '[TEST] 게이트확인'` · `phone 010-0000-0000` |
| **현장 귀속** | `site_slug='그랑라크-에일린의-뜰'` · `site_name` 일치 · `entry_path='/apt/그랑라크-에일린의-뜰'` · `interest_region='울산 남구'` |
| **pre_notice 분기** | `inquiry_type='진행상황알림'` ✅ — 「분양상담」으로 새지 않았다(미처리 경보 집계 분리가 살아 있다) |
| 부수 | `channel='organic'` · `birth_date='2000-01-01'` · `desired_type='미정'` · `marketing_ok=false` · `inquiry_count=1` · `status='new'` |

⚠️ **시트 «행» 자체는 내가 볼 수 없다.** 확인한 것은 Apps Script → `fn_insert_lead` → `leads` 까지다.
   시트는 그 뒤 단계이므로 Node 확인이 필요하다(삭제도 Node 몫).

## ⚠️ 대사 중 나온 것 하나 — 직전 테스트 행의 `birth_date = '2000-00-00'`
2026-08-29 의 「테스트」 행이 **월·일이 `00`** 인 날짜를 들고 있다.
내 이번 건은 `000101 → 2000-01-01` 로 정상 파싱됐으니 «지금 경로는 멀쩡하다» —
그 행은 생년월일을 부분 입력했거나 다른 경로로 들어온 것으로 보인다.
⛔ 손대지 않았다(리드 데이터). Apps Script 파싱 소관이라 **안건으로만 올린다.**

⛔ 이 스크립트는 **반복 실행하지 않는다.** `dedupe_key` 로 `inquiry_count` 만 올라
   「같은 사람이 두 번 문의했다」는 «다른 사실» 이 된다.


---

# ⛔⛔ 라이브 결함 — `get_apt_subscription_hub` 가 «모든 호출에서» 죽는다 (2026-08-30 발견)

## 증상
```
[apt/hub] rpc error: {"code":"42703","message":"column \"hero_license_tier\" does not exist"}
```
`/apt` 요청마다 찍힌다(01:32~01:36 연속, cache MISS·STALE 무관). 페이지는 200 이지만
허브가 `EMPTY_HUB` 로 떨어져 **청약 카드 섹션이 통째로 빈다.**
스모크 「/ — 데이터 띠 0칸」도 같은 뿌리로 보인다.

## 원인 — 함수 «정의» 안에 있다. 컬럼은 멀쩡하다
`apt_sites.hero_license_tier` 는 **존재한다**(확인함). 문제는 RPC 안이다:

```sql
-- base CTE 의 LATERAL: 이 목록에 hero_license_tier 가 «없다»
LEFT JOIN LATERAL (
  SELECT a.slug, a.hero_image_url, a.card_image_url,
         a.satellite_image_url, a.lifecycle_stage, a.builder
  FROM apt_sites a WHERE a.name = s.house_nm ...
) site ON true
...
-- shaped CTE: 그런데 여기서 «접두 없이» 참조한다
'site_slug', e.site_slug, 'hero_license_tier', hero_license_tier, 'thumb_url', e.thumb_url
```
JSON 키만 추가되고 **값의 출처가 안 따라왔다.** `enriched` 어디에도 그 컬럼이 없어
계획 단계에서 42703 이 난다 — 데이터와 무관하게 «항상» 실패한다.

## 고치는 법 (한 줄씩 둘)
1. LATERAL 의 SELECT 에 `a.hero_license_tier` 를 넣는다.
2. base CTE 가 `site.hero_license_tier` 를 내보내게 한다(그러면 `b.*` → `typed` →
   `enriched` 를 타고 흘러 `shaped` 의 맨 참조가 해소된다).

## ⛔ 내가 고치지 않은 이유
이 RPC 는 DB 담당/세션 A 자산이고, 저장소 `supabase/migrations/` 에 «정의가 없다»
(repo 에서 `get_apt_subscription_hub` 를 만드는 파일 0건 — 채팅으로 관리돼 왔다).
같은 함수를 두 곳에서 재정의하면 어느 판이 최신인지 알 수 없게 된다.
**지시를 주면 즉시 적용한다.**

## ⚠️ 오늘 스모크가 빨간불인 이유이기도 하다 (Rule #114 로 «완료 보고 안 함»)
```
❌ /            — 데이터 띠 0칸
❌ /blog?region=부산&sgg=해운대구 — 목록 0건 · 콘솔 「Connection closed」
```
⚠️ 블로그 쪽은 «데이터는 있다» — `v_blog_posts_listing` 에 해운대구 7건이 실재한다.
   같은 파라미터로 사상구는 31건이 뜬다. 렌더 도중 스트림이 끊기는 형태라
   위 RPC 장애와 같은 시간대에 몰려 있다. 원인 분리는 RPC 수리 뒤 다시 잰다.

## ⚠️ 함께 관측된 것 — DDL 직후 스키마 캐시 재적재
01:31~01:32 사이 `PGRST002 Could not query the database for the schema cache` 가
홈·블로그·상세에 «동시에» 떴다. 그 시각은 내가 D-2 컬럼과 카운트 RPC 를 넣은 직후다.
→ **낮에 DDL 을 치면 몇 분간 사이트 전역이 흔들린다.** 이 창은 지나갔지만(그 뒤 0건),
   앞으로 마이그레이션은 그 사실을 알고 친다. 42703 은 이것과 «다른 건» 이다 — 계속된다.


---

# 📌 규칙 2건 — 세션 A 는 «다음 기동 때 이 절부터» 읽을 것 (2026-08-30 · Node 승인)

## 1. RPC 소유권이 저장소로 넘어왔다. **DB 직접 수정 금지**
`get_apt_subscription_hub` 의 «수리 후 전체 정의» 를 저장소에 처음 등재했다:
`supabase/migrations/hub_rpc_baseline_2026-08-30.sql`

- 이후로 **이 파일이 진실**이다. 고칠 일이 생기면 파일을 고치고 마이그레이션으로 적용한다.
- ⛔ **DB 콘솔·채팅에서 `create or replace function` 을 직접 치지 않는다.**
  오늘 사고의 뿌리가 그것이다 — 정의가 저장소에 없어 「어느 판이 최신인가」를
  아무도 답할 수 없었고, 그래서 나는 «고칠 수 있는데도» 손을 못 댔다.
- 같은 상태인 다른 RPC 가 있으면(정의가 repo 에 없는 것) 같은 방식으로 등재해 나간다.
  ⚠️ 등재할 때 정의는 **`pg_get_functiondef` 를 base64 로 받아 옮기고 md5 로 대조**한다.
     손으로 옮겨 적으면 그 오타가 «새 결함» 이 된다.

## 2. DDL 은 «트래픽 저점» 에 몰아 친다
2026-08-30 01:31~01:32(KST 10:31~10:32) — 내가 컬럼 하나와 집계 RPC 하나를 넣은 직후
`PGRST002 Could not query the database for the schema cache` 가 **홈·블로그·상세에 동시에** 떴다.
PostgREST 가 스키마 캐시를 다시 읽는 동안 살아 있는 요청이 함께 흔들린다.

> ⛔ **낮에 DDL 을 치지 않는다.** KST 02~04시대(크론이 도는 04:50 «전») 또는 배포 윈도우에 몰아 친다.
> ⚠️ 급한 수리는 예외지만, 그때도 **친 시각을 기록**한다 — 그래야 몇 분 뒤의 이상 로그를
>    「새 결함」으로 오인하지 않는다. 오늘 42703 과 PGRST002 를 가른 것이 정확히 그 기록이다.
