# 설계서_U1a_20260829 — /apt/[id] 상세 개편 (U-1층 1차)

> **문서 지위**: U-1층(`docs/U_20260829.md` §3)의 «1차 실행분» 단독 설계서.
> §1 실측·§2·§4 는 채팅이 완료·소거했고, 아래는 **정본으로 수령한 잔여 절**이다(2026-08-29).
> 상위 기준: 마스터 `MASTER_20260829.md` > `U_20260829.md` > 이 문서.

---

## 3. 배치표 (실행 목록)

- **전 섹션 펼침**: `DetailSection` 을 `details`→`section` 전환(caret·summary 제거, `id`·scroll-margin 유지), `AccordionEnhancer` 퇴역.
  FAQ 문답 아코디언·댓글 더보기는 **유지**(경계 근거를 커밋 메시지에 기록).
- **점프바(`SiteJumpBar`)**: 칩 4→5 — 「신청」 추가(리드폼 앵커), Enhancer 연동 제거→**순수 앵커**, sticky 여부 실측 후 **sticky 42px** 확정.
- **리드폼은 이동하지 않는다**(이미 상단, A6 배치 존중) — 시각만 DS **틴트 색면형** 승격, `InterestRegisterHero`(회원 CTA)와 **비인접**(S7-3) 유지.
- **히어로 캡션**에 `expected_sale_period` + `VerifiedBadge` 추가(§7-1 한정어 렌더) · **`AptKeyMetrics` 수치별 confidence 뱃지** 연동(conflicting=「출처 충돌」) · **지도·차트 고정 높이**(CLS 0) · **history 섹션은 생애 타임라인 스타일로 승격**(permits 이벤트 편입은 후속).
- **데스크탑 레일**: 펼침으로 본문이 길어지므로 `SiteDetailRail` sticky-follow 경계(H6-2) **재검증 필수**.
- 이 화면 몫 **하드코딩 동반 회수**(라운드·gap·인라인 — 상세는 최대 잔여 지대) + **I-3 이미지 캡션 라벨**.
- **게이트 전종** + 접힘 해제의 **추가 fetch 0 확증** 명기.
- 완료 보고 = **6폭×3모드 확인 목록** = **중단점 D 리뷰 요청**. 통과 전 후속 화면(어드민·I-2) 착수 금지.

## 5. 시각

- **DS 토큰만 · 신규 발명 0.**
- **카드 남발 금지** — 밀도 높은 표는 «플랫 + 구분선», 카드는 «강조 블록»(수치·폼·노트)에만.
- 조밀 표는 `--fs-2xs`(용도 제한 준수).
- 이 화면 몫 하드코딩 동반 회수(라운드·gap·인라인 — **상세는 최대 잔여 지대**).

**집행 실측 (2026-08-30)** — 상세가 실제로 렌더하는 35개 파일을 대상으로:
- 여백 **55건** · 라운드 **23건** → 사다리 토큰(`--sp-*` · `--radius-*`)으로 회수. 22개 파일.
  숫자를 «올림» 한 것이 아니라, 박혀 있던 값 대부분이 **font-small 모드의 값**이었다
  (6·10·14). 토큰으로 바꾸니 base 8·12·16 / small 6·8·12 로 «모드를 되찾았다».
- 카드 안쪽 `padding: 14` 2건은 사다리가 아니라 **`--card-p`** 가 주인이다(16/14/20).
- ⛔ `borderRadius: 1` 2건은 «단» 이 아니라 1px 막대의 모서리다. 그대로 둔다.

**⚠️ 남긴 것 — 문자열 단축 여백 약 200건 (`padding: '5px 6px'` 계열)**
> ⛔ **기계적으로 반올림하지 않는다.** 이 값들은 «사다리 밖» 이 아니라 **사다리 «아래»** 다.
> 4px 바닥 위에서 1~3px 로 조절된 조밀 표·칩 여백이라, 8px 로 올리면 **42px 행 체계가
> 그 자리에서 무너진다** — 판정 ⑧이 전역 `min-height: 44` 를 막은 것과 «같은 이유» 다.
> → 자리별로 「조밀 표인가 아닌가」를 보고 갈라야 하는 작업이라 **별건 안건**으로 등재한다.

## 6. 성능

- **펼침의 fetch 영향**을 커밋에 명기 — 판정 ②로 문구를 정정했다:
  **「데이터 fetch 0 + 라우트 프리페치 정상」**.
  ⚠️ 실측: 스크롤 시 17건이 늘지만 «전부 Next `<Link>` 라우트 프리페치» 이고
     `/api/*`·데이터 호출은 **0건**이다. 펼침으로 링크가 보이게 되어 다음 페이지를
     준비하는 것이지 이 페이지가 더 읽는 것이 아니다. **D 안건에서 제외됨.**
- 지도·차트 **고정 높이**(CLS 0).
- 전/후 **스크롤 총높이 · LCP** 측정 보고.

## 7. 검증

- 게이트 전종: `token-snapshot` · `contrast-audit` · `font-large-guard` · `touch-spacing-audit`(**펼침으로 신규 노출된 요소 44px 포함**) · `ds-6x6`.
- **ad-safety 경로 실측** — 광고 파라미터 진입 시 미확인 숨김 + 폼 정상.
- **상단 폼 리드 E2E 1건.**
- 완료 보고 = 6폭×3모드 확인 목록 = **중단점 D 리뷰 요청**.

## 8. 범위 밖 (후속 소커밋)

`permits` 이벤트 타임라인 실편입 · GEO 요약 블록·FAQ JSON-LD · U-2층 구독 CTA · `DetailShell` 퇴역(Rule #19).

## 9. I-3 이미지 캡션 라벨

모든 현장 이미지에 **성격 라벨**을 붙인다 — 「위성사진」·「시행사 제공」·「현장 촬영」.
조감도는 「**조감도(시행사 제공) — 실제와 다를 수 있음**」.
**`alt` 도 동일**하게 간다.

---

## 부록 — 실행 중 발견 (U-1a)

### A-1. 확신도 어휘가 «설계와 구현에서 갈려 있다» (2026-08-29)

| 출처 | 값 |
|---|---|
| 설계 `PV_INSTRUCTION §D6` | `verified`(독립 출처 2개 일치) · `estimated`(단일 출처) · **`conflicting`**(출처 충돌) · `rumor` |
| 구현 (`apt_permits` DB 제약 · 코드 리터럴) | `verified` · `estimated` · **`confirmed`** · `rumor` |

`confirmed` 는 D6 에 **없고**, `conflicting` 은 구현에 **0건**이다.

⛔ DS-2a 에서 CC 세션 B가 「`conflicting` 은 검수 큐 이름이지 확신도가 아니다」로 **단정했고 그것은 틀렸다** —
마스터의 「conflicting 큐」만 보고 D6 를 확인하지 않은 결과다.

→ **화면은 둘 다 렌더한다.** 어느 쪽도 「미확인」으로 떨어뜨리지 않는다.
→ **어휘 통일은 PV 트랙 판단**이다(제약에 `conflicting` 추가 / D6 에 `confirmed` 추가 중 택1).
   화면의 몫은 «오는 값을 정직하게 표시» 하는 것까지다.
