# 최종지시서_PV_20260829 — 분양예정 파이프라인(P) · 기존 데이터 후검증(V)

> **문서 지위**: 2026-08-29 대화의 P0~P4·V 논의 전체를 대체하는 단독 실행본.
> 작성 전 의무 절차 이행: git log 30개(b3a66ce~) · 과거 대화 검색(2026-04 선점 설계 2건) · DB 실측 완료.

---

## 0. 병행 전제 (모든 커밋 공통)

- 착수 전 `git pull --rebase`. 새 Rule 번호는 origin 확인 후 부여. 세션 종료 시 STATUS.md 갱신.
- **vercel.json 현재 crons 79개** — 신규 크론 라우트는 이 지시서 전체에서 **최대 2개**(`permits-sync`, `verify-facts`). 나머지는 기존 크론 확장. 변경 시 Rule #112(개수 기록) 준수. functions 상한 초과로 배포가 막혔던 사고(b719486, 51→37) 재발 금지.
- 배포 후 스모크는 H7-0 체계 재사용.
- Rule #19(크론 삭제 전 3중 검증) · #49(/apt/[id] 병렬 fetch 뭉치에 추가 금지 — 이 지시서 작업은 전부 크론·DB 측) · #68(build+tsc 둘 다).

**롤백 전략**: 코드는 태그 `pre-pv-20260829`. 데이터는 permit 유래 레코드 전량이 `stage_source='permit'`으로 식별되므로 사고 시 `is_active=false` 일괄 격리 → 크론 비활성. `apt_permits` 스테이징은 **드랍 금지**(원시 보존이라 재실행 자산). P0의 DB 변경은 실행 전 대상 행 백업 SELECT를 STATUS 부록에 남긴다.

## 1. 배경 실측 (요약 — 재현 쿼리는 부록 A)

| 실측 | 값 |
|---|---|
| pre_announcement 전국 | **27건** (부산19·경기3·경남2·인천2·울산1 — 서울·대구 0) |
| 그 출처 | 부울경 22건 중 20건 = `seed:web-2026-08-24` **하루짜리 수동 시드**, source_ids 대부분 `{}` |
| 아실 부울경 분양예정 56곳 대조 | 카더라 존재 25 / **결측 31** (경남 8/8 전멸, 울산 14/16 결측) |
| 울산·경남 정비 레코드 | 17건 전부 `migration:redevelopment_projects` 이관 잔재 — 동 단위 개괄, 구역 단위 소스 없음 |
| upcoming_projects (2026-04 설계) | 32건, **2026-04-14 이후 갱신 0** — 방치된 이원 저장소 |
| 중복 레코드 | 문현3·대연역·괴정3·삼보·초량3 각 2건 (시드가 기존 정비 레코드와 별개 생성) |
| lifecycle-stage-refresh (pg_cron) | **active=false** — 원인 미부검 상태 |

**구조 진단**: 기존 파이프라인은 전부 공식 이벤트 *이후*(모집공고·실거래·정비 고시)를 수집. 경쟁 앱의 분양예정 리스트는 *인허가 단계*를 수집(레코드명 "화정동638-3신축" = 지번+용도가 증거). 수동 시드는 두 번 시도됐고 두 번 다 부패했다(4월 upcoming_projects, 8월 seed:web). **결론: 세 번째 수동 시드는 금지하고 자동 소스를 붙인다.**

## 2. 기존 자산과의 관계 — 중복 설계 금지

| 기존 자산 | 이 지시서에서의 처리 |
|---|---|
| `upcoming_projects` + `blog-upcoming-projects` 크론 | **apt_sites를 분양예정의 단일 진실로 확정(D1)**. 테이블은 P1 가동 확인 후 폐기 권고 — 존치 여부는 Node 결정(§4) |
| 2026-04 선점 설계의 소스 4·5순위(HUG·세움터 인허가) | 당시 미구현 — **P1이 이것의 완성**. 1순위(시공사 페이지)는 `builder-site-sync`로 이미 존재 → V2 브랜드 소스로 재사용 |
| `crawl-busan-redev` · `crawl-nationwide-redev` 등 정비 크롤러 | 정비 축 담당 유지. P1은 분양예정 축 — 역할 분리, 같은 레코드는 P2 매칭으로 접합 |
| **stage 기록자 규약 (H7-2, 5ca1eee "같은 컬럼에 기록자가 둘")** | P1·P2는 `lifecycle_stage`를 직접 덮어쓰지 않는다. `stage_source` 명기 + `stage_locked` 존중 + 승격은 stage-derive 규약 경유. CC는 착수 시 H7-2 규약 원문을 먼저 읽고 충돌 시 중단·보고 |
| C v2 관측(`apt_observations`) · `issue-preempt` · `issue-fact-check` | P1 신규 인허가 이벤트 = 관측 1종으로 적재(현장 상세·지역 허브·/feed 자동 콘텐츠 + 이슈 선점). V 크론의 AI 추출은 issue-fact-check 인프라 확장을 우선 검토 |
| `apt-parse-announcement` | 공고 후 병합 단계에서 재사용 |
| 조감도 웹 수집 (4월 설계 잔재) | **부활 금지** — 이미지 정책(시행사 서면 허락만) 유지 |

## 3. 확정 결정

- **D1. 분양예정 단일 진실 = apt_sites.** 이원 저장소 신설 금지. 원시 보존용 스테이징 `apt_permits`만 예외.
- **D2. 수동 시드 금지.** 예외는 P0-1의 공고 임박 2건뿐. 나머지 결측 29곳은 P1 첫 실행이 잡는다.
- **D3. stage 축 규약**: 정비 축(union_established~mgmt_approved)과 분양 축(pre_announcement~)이 한 enum에 섞여 있는 것이 중복 5쌍의 구조적 원인. 병합 후 한 레코드가 두 축을 갖지 않는다 — 관리처분 + 분양 신호(허가·보증·시공사 분양페이지·공고 승인) 감지 시 pre_announcement로 **승격**(stage_source 기록, stage-derive 경유).
- **D4. 자동 반영 경계**: `name_variants`·`display_name`·`builder` = 자동 / `lifecycle_stage`·세대수·가격·`slug` = 검수 큐. slug는 어떤 경우에도 불변(색인 자산).
- **D5. 파워링크 프리즈**: 9/11 재논의까지 D_기축·E_대표 불변. 신규 분양예정 키워드는 A_분양 그룹에 수동 추가만.
- **D6. 정확성의 표기**: confidence 값 체계 = `verified`(독립 출처 2개 일치) / `estimated`(단일 출처) / `conflicting`(출처 충돌) / `rumor`. 판정 근거·출처·일자는 confidence_note에 기록.

## 4. 역할 분담

**Node**
1. 공공데이터포털 활용신청 — 건축HUB 건축인허가정보 서비스 (단, P0-5의 APT_DATA 키 용도 확인 결과를 먼저 받을 것)
2. 파워링크: P0-1 시드 2건의 키워드를 A_분양에 추가 + 연결 URL
3. 결정 2건: upcoming_projects 폐기 여부 / 중단점 A·B 리뷰

**채팅** — §5 P0 전체(코드 0, DB·조사만), 중단점 검증, V 검수 큐 운영
**Claude Code** — §6 PV-1~PV-5

## 5. P0 — 채팅 몫 (즉시)

- **P0-1 긴급 시드 2건** (실행 직전 웹 재검증 의무 — 세대수·시공사·공고일):
  - 울산 그랑라크 에일린의뜰 — 남구 야음동, 1,521세대, 공고 D-3
  - 문수로 비스타더파크 — 남구 무거동, 700세대, 공고 D-14
  - `stage_source='manual:urgent-20260829'`, source_ids에 출처 URL 필수, name_variants에 붙임/띄어쓰기 변형. **공고 후 청약홈 레코드와 병합 확인**을 후속 체크로 등록(시드가 세 번째 중복을 만들면 안 된다).
- **P0-2 중복 5쌍 병합** (문현3·대연역·괴정3·삼보·초량3): canonical = ①공공 소스 계보(redev/청약홈 연결) → ②content_score → ③생성일. 시드 중복분은 `is_active=false`, 이름은 canonical의 name_variants로 흡수, source_ids 승계. slug 불변. **병합 전 파워링크 대조 필수**: 대상 레코드에 등록 키워드가 걸려 있으면 연결 URL을 canonical slug로 교체 — 슬러그 불일치로 /apt/search에 튕긴 3건 실증(PL-0)의 재발 방지.
- **P0-3 브랜드 주입 — 검증 확정 3건만**:
  - 명륜2 재건축 → display_name **래미안 마크 더 스위트** (삼성물산 2021.6 선정, 2025.10 변경인가 504세대 — 세대수 반영)
  - 남천2-3(삼익비치) → **그랑자이 더 비치**
  - 가야1 재개발 → **더 다이너스티 가야** (HDC·대우 컨소, 세대수 1,943→**1,901 변경 예정** — note에 병기. 경쟁 앱은 구값 1,943 사용 중)
- **P0-4 conflicting 큐 1·2호**: 구서3(DL이앤씨 확정이나 공식 361세대 vs 앱 415세대 충돌, 펫네임 미확인) / 울산 B-07(HDC 확정, 펫네임 미정, 세대수 1,295~1,391 산포, 2024.12 조합장 교체로 인가 지연) — `confidence='conflicting'` + note에 근거. **stage 검수 1건 병기**: 울산역신도시 동문디이스트 — post_move_in 오분류 의심(외부 기준 분양예정 미정). D4에 따라 자동 수정 금지, 검수 큐에 등록만.
- **P0-5 조사 4건**: ① env PENDING의 APT_DATA 키가 인허가 API 용도인지 ② lifecycle-stage-refresh(pg_cron)와 stage-derive(vercel 크론) 관계 부검 — cron_logs·git 이력. **부검 전 재가동 금지** ③ upcoming_projects 32건 내용 검토 → Node 폐기 결정 상신 ④ apt_sites에 **분양예정 시기 필드** 존재 확인(없으면 PV-1 마이그레이션에 `expected_sale_period` 추가 상신) — 인허가 API에는 분양시기가 없어 V·HUG·시공사 소스가 채우는 필드이자, 앱들이 보여주는 "2026년 10월 분양예정" 표기와 P4 입찰 트리거의 입력.

## 6. Claude Code — 커밋 계획

### PV-1: 공유 지역코드 모듈 + 스테이징
- crawl-apt-trade의 LAWD_CODES를 공유 모듈로 추출(신규 크론과 공용). **알려진 결함을 주석으로 명기**: 안산 부재 · 창원 부분 · '수원영통'/'청원구' 오라벨 — D5 트랙과 코드 테이블 단일화, 이 커밋에서 값 수정은 하지 않는다(백필과 짝이므로).
- `apt_permits` 스테이징 마이그레이션: 관리대장PK unique, raw jsonb 원시 보존, matched_site_id, match_score, processed_at. apply_migration 사용.

### PV-2: `permits-sync` 크론 (신규 라우트 1/2)
- 소스: 국토교통부 건축HUB **건축인허가정보 서비스**(공공데이터포털). 채팅이 앱 설명 기반으로 추정한 필드(허가일·착공예정일·세대구/호수·주용도·구역명·대장PK) — **CC가 실스펙 대조 후 매핑 확정. 추정과 다르면 중단·보고.**
- **게이트 실측 확정(2026-08-29, R-3 — 위 추정을 대체)**: ①두 트랙 필드명 상이 — house(mgmHsrgstPk·totHhldCnt·apprvDay·useInsptDay·purpsCdNm) vs arch(mgmPmsrgstPk·hhldCnt·archPmsDay·useAprDay·mainPurpsCdNm), 매핑 2벌 필수(한 벌 뭉뚱그리면 arch 전량 유실이 「API 부재」로 위장) ②착공예정일은 YYYYMM 6자리 — 날짜 변조 없이 expected_sale_period 월 정밀도로 직결 ③응답 봉투 XML(데이터有)/JSON(무) 혼합 — NO_CODE를 키 실패로 오독 금지 ④실질 한도는 초당(코드23) — 350ms 간격+재시도(23·05·429·503만, 30·22는 한도 소모라 제외)를 fetch 단일 지점에 ⑤**읍·면 단위 조회는 리(里)를 포함하지 않는다**(기장읍 25건 vs 동부리 단독 658건) — 법정동 리 레벨까지 전수 순회 필수, 아실 결측의 「청량읍 덕하리」류 군 지역 현장이 이 규칙의 실증 ⑥부울경 전수 = 2,834동×2트랙 = 5,668호출/일(한도 10,000 이내)이나 350ms 간격으로 약 33분 — 단일 크론 300초 초과, **청크 회전(커서 저장·다회 실행) 설계는 중단점 A 안건**.
- 순회: 공유 코드 모듈 × 페이징. 쿼터 실확인 후 백필 계획(허가일 최근 36개월, 사용승인 전) → 증분 일 1회.
- 에러 규율(실거래 사고 이식): res.ok + resultCode 검사 — **정상값을 가정하지 말고 실호출로 확인**(실거래는 '00'이 아니라 '000'이었다). 실패 코드를 cron_logs에 접두어 `permits:`로 기록. `if(!error)` 침묵 패턴 금지.
- 필터: 주용도 공동주택 계열, 세대수 ≥ 30.
- **검증 게이트(핵심 리스크)**: 30세대 이상 아파트는 주택법상 **사업계획승인** 트랙이라 건축법 인허가 API에 없을 가능성이 있다. 표본 **5건** = 소형 3(**화정동 638-3 · 옥교동 224 · 범천동 1090-61**) + 대단지 2(**그랑라크 에일린의뜰 야음동 1,521세대 · 문수로 비스타더파크 무거동 700세대**). **[2026-08-29 재판정: 검출 2(화정·옥교, house) · 오검출 2(느슨한 needle이 문수로대공원·무거비스타동원에 걸림) · 미측정 1(범천동, 부산진구 스코프 밖). 그랑라크는 별도 실증으로 검출 확정 — 야음동 350-5 「울산 남구 B-14 주택재개발 정비사업」 1,521세대 정확 일치. 문수로는 원문 부재 = 인허가 전 단계(보도만 존재) 실물 1호로 V2 영역 실증. 핵심 발견: 원문은 브랜드명·사업명이 아니라 「구역명」으로 온다 — 이름 축 도달 불가, 지번·세대수가 유일한 연결고리. 게이트 규율 정정: needle 이름 일치만으로 통과 금지, 세대수 대조 의무화.]**

**[중단점 A]** 첫 수집 리포트: 표본 3건 존재 여부 · 부울경 결측 31곳 커버율 · 레코드 품질(주소/세대수/일정 충전율) → Node 리뷰 후 PV-3 진행.

#### [중단점 A — 2026-08-29 통과] 실측 요약과 안건 7건 판정

수집 범위: 우선순위 21시군구 · 법정동 1,171/2,834(41%) · 2트랙 · 실호출 2,540 · dry-run.

| 항목 | 실측 |
|---|---|
| 봉투 | OK 2,300 · HTTP_503 21 · EMPTY_BODY 21 · **NO_CODE 0**(42건 완전 해소) · 429 0 |
| house | 원문 12,977 · 후보 **721** · 0건 법정동 749/1,171(64%) |
| arch | 원문 96,920 · 후보 **33** · 0건 법정동 20/1,171 |
| 리(里) | 후보 **207/754 = 27%** ← 리를 뺐다면 사라졌을 몫. §6 ⑤ 실증 |
| 코드 불일치 | 0 (D5-4 정상) |
| arch 주용도 | 공동주택 18 · 근생 9 · 판매 3 · 업무 2 · 교육연구 1 (노이즈 15) |
| 허가연도 | 754건 중 **82%가 「최근 36개월」 창 밖**(최고령 1994년) |

**안건 판정 (Node)**
- **①** 청크 회전 → **커서 테이블**. permits-sync 의 bjdongCd 미반영·maxDuration 300s 를 이 안건으로 병합(전수 99분 실측). **[2026-08-29 구현 완료 — `apt_permit_cursor`]**
- **②** arch → **월 1회 차등**. 부수: 일 10,000 이 HsPms·ArchPms 별개 활용신청인지 **활용신청 화면에서 실확인**.
- **③** 백필 시간창 → **매칭 시점 필터**. 창 밖은 버리지 않고 `out_of_window` 표기 보존(원문 보존 취지). **[PV-3a 구현 완료 — `isOutOfWindow`]**
- **④** 표본 규율 → 위 §6 재판정으로 **정정 완료** · 세대수 대조 의무화.
- **⑤** 매칭 → **지번+세대수 1순위 확정** · 구역명을 name_variants 로 주입(D4 경계 안). **[PV-3a 구현 완료 — `judgeMatch`]**
- **⑥** EMPTY_BODY → **위치 기록 + 재조회 큐 + 커버율 분모를 「측정된 동」으로**. **[2026-08-29 구현 완료]**
  **검증 1호 확정: 울산 남구 달동(후보 0건) · 남구 B-07(구역명 0건)** — B-14 는 잡히는데 이 둘이 0인 것이
  「진짜 0」인지 구멍인지 판정되지 않는다. 결측 명단 2건(달동더리브·대상웰라움달동)이 여기 걸려 있다.
- **⑦** 희소 법정동 → **house 0건 동 90일 캐시**(영구 스킵 금지 · arch 미적용). **[2026-08-29 구현 완료]**

**프로덕션 배포 확인 (11커밋 1배포)**: 인증 없이 401 · `?dry=1&codes=31170` HTTP 200 ·
`api_calls 2 = expected 2` · `error_codes {}` · `code_mismatch 0`.
⛔ 그때의 `items 0` 은 **bjdong 미구현의 정상 증상**이었다. 그 dry 확인의 유효 범위는
   «봉투 형식 · 인증 · 키 등록» 까지다 — 0을 통과로 읽지 않는다. 안건 ① 적용 후 재확인한다.

**첫 실가동 3단계 (자정 쿼터 리셋 후)**: ①프로덕션 `?dry=1` 1회 → ②`?codes=` 1~2 시군구 실적재 검증
→ ③전체 순환. 실적재 개시 후 **달동·B-07 재조회 = 검증 1호**.

**부분 검출 4건은 PV-3 실전 케이스로 이관**: 더파크 비스타동원(신정동 563-1 유력) ·
무동 블록 대응 · 내이3지구 variants · 우정동은 명단 정보 부족으로 유보.

### PV-3: 매칭·upsert (P2)
- match_apt_site 확장: Kakao 지오코딩(지번 주소 보유) + 법정동 + 세대수 ±15% + 이름 유사도 — **지번·세대수가 1순위, 이름은 보조**(재판정 실증: 원문은 「구역명」으로 와서 브랜드명·사업명 어느 축으로도 닿지 않는다. 구역명은 name_variants 주입 경로로 흡수 — D4 자동 반영 경계 안). 같은 사업의 동별 허가·변경허가 이력 레코드는 대장PK 기준 집계·최신 선택 후 매칭.
- 매칭 성공 → 기존 레코드 갱신(source_ids에 permit PK 추가, 빈 필드만 보강, stage는 D3 규약 경유). 미매칭 → 신규 생성: `stage_source='permit'`, `confidence='estimated'`, name은 지번형 표준 "{법정동} {지번} 공동주택".
- **색인 게이트**: 신규 레코드가 content_score 배치 대상에 들어가는지, data_quality_score 세팅, sitemap 편입선(cs≥25)·noindex 조건을 실확인. 만들고 색인 안 되면 무의미.
- **수명 규칙(좀비 방지)**: 인허가는 무산·장기 지연이 흔하다 — permit 유래 pre_announcement가 착공예정일 경과 후 180일 무변화면 '보류'로 강등(노출 축소, sitemap 존치 여부는 그때 판단), 사용승인 감지 시 기축 축으로 승격. 만료 규칙 없이는 죽은 분양예정이 쌓여 목록 신뢰를 갉는다.

### PV-4: 갭 워치 (P3)
- v_seo_daily_snapshot에 5지표 추가: pre_announcement 수 · permit 미매칭 수 · conflicting 수 · 중복 후보 수(이름 유사+동일 법정동) · 단계 정체 수(180일 무변경 정비 레코드). 재현 쿼리를 부록 A 형태로 저장.

### PV-5: `verify-facts` 크론 (V, 신규 라우트 2/2 — issue-fact-check 확장으로 흡수 가능하면 라우트 0 추가)
- 큐 우선순위: 파워링크 착지 현장 → 큐레이션 → pre_announcement 전량(seed:web 20건 + source_ids `{}` 1순위) → 리드 발생 현장 → 나머지 순차 일 N건.
- 소스: 네이버 뉴스/웹 API(naver-sc-sync 자격 재사용) "{구역명} 시공사/단지명" + builder-site-sync 산출. AI 추출은 기존 인프라 재사용.
- 판정: 필드별 — 독립 출처 2개 일치 → `verified` 승격 + display_name/variants/builder 자동 반영(D4 경계). 충돌 → `conflicting` + 검수 큐. note에 출처·일자. **'독립 출처'는 매체 수가 아니라 원출처 기준** — 같은 보도자료 받아쓴 기사 n개는 출처 1개다(공시·조합 공고·시공사 발표 vs 언론 재전달을 구분).
- 쿼터: 네이버 검색 API 일한도를 keyword_rank_daily와 배분해 검증 큐의 일 처리량을 잔여 쿼터 안에서 산정 — 순위 추적을 밀어내지 않는다.
- 블로그 기존 글: **본문 불변**(규칙). C2 apt_site_id FK + 메타(blog_meta_rewrite 경로)만.

**[중단점 B]** V 첫 배치 20건 결과 리뷰 → 자동 반영 범위 재확정. (선택 후속: 검증 뱃지 노출 "출처 n건 교차확인 · 날짜" — 별도 소커밋, 급하지 않음)

## 7. P4 — 9/11 파워링크 재논의에 부의

공고 임박 신호(permit 착공예정일 접근 · HUG 분양보증 · 공고 승인) → 검색광고 API로 키워드 자동 등록(A_분양) + 입찰 상향 룰. 메모 ⑥(26H2·27H1 분양예정 입찰 상향)의 **대상 목록은 PV-2 산출로 자동 생성** — 그 전 수동 목록 작성 금지. HUG 조사 완료(2026-08-29, 채팅): **사업장 단위 발급 리스트 공개 API 없음** — 공공데이터포털은 지역×분기 집계(분양보증 발급현황)·보증사고 이행 현황뿐이고 HOUSTA 개방포털도 동일. 보증기간 정의상 발급≈입주자모집공고 승인 시점이라 선점 리드타임도 짧다. 따라서 **P4 트리거에서 HUG 제외** — 트리거는 ①permits 착공예정일 접근 ②청약홈 공고 승인(기존 크론) ③builder-site-sync 3종으로 확정. HUG 분기 집계·민간 아파트 분양가격 동향은 브리핑·블로그 콘텐츠 소스 후보로 이관.

## 7-1. 표기·컴플라이언스 — 신규 pre_announcement 페이지 공통 규칙

- **미확정 정보의 한정 표현 의무**: 예상 분양가·예상 시기 등 확정 전 수치는 "예상/추정" 한정어 + 출처 + 기준일을 함께 표기하고 confidence 값과 연동해 렌더. 확정 표기는 verified만. 분양 광고로 오인될 표현(확정 분양가·"분양 개시" 등) 금지 — 표시광고 리스크의 1차 방어선이자 "카더라" 포지셔닝의 원칙.
- **전화번호 미공개 정책 유지**(2026-08-26 확정): permit 유래 신규 페이지에도 동일 — 전화 CTA 없이 관심고객 폼 유도.
- **공공데이터 출처 표기**: 인허가 유래 데이터는 "출처: 국토교통부 건축HUB" 표기(VWorld 출처 표기 관례를 따름). 데이터 이용조건은 CC가 활용신청 화면에서 실확인해 STATUS에 기록.

## 8. 검증 계획

1. PV-3 완료 후 본 대화의 부울경 56곳 리스트로 재대조(부록 A-2) — 목표: 결측 31 → 한 자릿수. API 특성상 못 잡는 유형(사업계획승인 전용)은 예외로 기록하고 폴백 경로 판단에 사용. **[2026-08-29 실측(21시군구): 결측 27 중 검출 7 · 부분 4 · 미검출 16. 미검출 상당수는 건축허가 전 단계로 추정(검출 8건 전부 허가 2019~2026, 미검출은 원문 부재). 목표 재해석 확정: 한 자릿수는 P1 단독이 아니라 P1+V2(뉴스)+청약홈 합산으로 판정 — V 보강 필요성은 중단점 B 판단. 달동 0건(명단 2건 소재)·B-07 0건은 안건 ⑥ 수리 후 재조회 검증 1호.]**
2. 브랜드 주입 14일 후 keyword_rank_daily에서 브랜드 키워드(래미안 마크더스위트 · 더 다이너스티 가야 · 그랑자이 더 비치) 순위 진입 관찰.
3. 갭 워치 5지표 주간 추이 — pre_announcement가 시드처럼 정지하면 파이프라인 사망 신호.

## 9. 범위 밖

작업 메모 ②디자인 ③정보밀도 ⑤주식 리뉴얼(→DS 트랙 흡수) · ①네이버 웹문서 ④캐러셀(→U 설계서 노출 축 편입) / 블로그 본문 수정 / 조감도·이미지 수집 / D5 실거래 트랙 자체 / **경쟁 앱(아실·호갱노노) 스크랩 — 금지, 수요 신호는 검색광고 API 월간검색량으로 대체**.

## 부록 A — 재현 쿼리

**A-1 원인 실측**
```sql
-- 분양예정 출처 감사
SELECT name, region, stage_source, confidence, created_at::date,
       left(COALESCE(source_ids::text,'-'),60) AS src
FROM apt_sites WHERE lifecycle_stage='pre_announcement' ORDER BY created_at;
-- 지역 분포
SELECT region, count(*) FROM apt_sites
WHERE lifecycle_stage='pre_announcement' AND is_active IS NOT FALSE
GROUP BY 1 ORDER BY 2 DESC;
```

**A-2 커버리지 대조 템플릿** (외부 리스트 대조 시 targets만 교체)
```sql
WITH targets(n,label,reg,pat) AS (VALUES
 (1,'현장명','부산','이름패턴')  -- 56곳 원본은 아래 A-2b
)
SELECT t.n, t.label,
 (SELECT count(*) FROM apt_sites s
   WHERE (s.name ~* t.pat OR COALESCE(s.name_variants::text,'') ~* t.pat)
     AND s.region ~* t.reg) AS cnt
FROM targets t ORDER BY t.n;
```

**A-2b 대조 원본 — 부울경 분양예정 56곳 판정 (2026-08-29 기준)**
*결정이 대화에만 있으면 안 읽힌다는 자체 규칙에 따라 명단을 문서에 고정한다. §8 재대조의 targets 원천.*

- **존재 20**: 금곡2-1 · 문현3(중복2) · 더샵 아시아드 엘포르타(=거제2지주택) · 대연역가로(중복2) · 부산범천동주상복합 · 해운대중동동원로얄듀크 · EDC 32BL · 온천2차가로 · 괴정3(중복2) · 서면서희스타힐스 · 삼보가로(중복2) · 연제갤러리자이 · 부산한양프라자 · 대신해모로센트럴2차 · 괴정5 · 부전역소규모 · 거제역동원로얄듀크 · 초량3(=호반써밋, 중복2) · EDC 금강펜테리움6BL · 울산역KTX역세권힐스테이트
- **구역명만 4 + 오분류 1**: 명륜2(=래미안 마크더스위트) · 구서3(=DL, conflicting) · 남천2-3(=그랑자이더비치) · 가야1(=더 다이너스티 가야) / 동문디이스트(post_move_in 오분류 의심 — P0-4 검수)
- **결측 31 — 경남 8**: 용원하버시티에일린의뜰 · 사천송지엘크루 · 밀양내이동2차 · 밀양나노융합유승한내들 · 거제장승포서희스타힐스 · 다인로얄팰리스부산신항2차 · 통영한림풀에버 · 창원무동지구2차동원로얄듀크(1차만 존재)
- **결측 — 울산 14**: 울산남구B-07 · 울산그랑라크에일린의뜰(→P0-1) · 문수로비스타더파크(→P0-1) · 울산우정동주상복합 · 울산달동더리브 · 울산다운2지구C-1블록 · KTX울산역복합특화단지A3블록 · 울산더파크비스타동원 · 화정1지구서한이다음 · 옥교동224일원주상복합 · 화정동638-3신축 · 야음동공동주택 · 청량읍덕하리공동주택 · 대상웰라움달동
- **결측 — 부산 9**: 시민공원비스타동원2차 · 범천동1090-61일원 · 에코델타시티29BL · 쌍용더플래티넘센텀(망미) · 연천시장정비사업 · 남부민동주상복합 · 다대동구획지(주공업부지) · 부산명지2지구B11예미지 · 부산명지2지구B12예미지

**A-2c 21시군구 스코프 교차 실측 (2026-08-29 · 지번·세대수·구역명 기준)**
*⚠️ 브랜드명 단독 일치는 근거로 세지 않는다 — 표본 오검출 2건이 그렇게 났다.*

- **검출 8**: 그랑라크(야음동 350-5「울산 남구 B-14 주택재개발 정비사업」1,521 정확일치) · 화정동 638-3(지번) · 옥교동 224(지번) · KTX울산역 A3(「울산KTX역세권복합특화단지 A3블록」1,320·2025-06-27) · 울산다운2지구 C-1(「울산 다운2지구 C-1BL 공동주택」644·2023-09-14) · 야음동 공동주택(803·2022-10-25) · 밀양 나노융합 유승한내들(부북면 운전리「밀양 유승한내들」745·2021-09-14) · 남천2-3(「남천2구역(비치아파트)」3,060·2022-09-29 · 구역명만 4 중)
- **부분 4(PV-3 실전 케이스)**: 울산 우정동 주상복합(2023년 허가 4건 후보) · 울산 더파크 비스타동원(신정동 563-1, 475세대, 2026-02-11) · 창원 무동지구 2차(북면 무동리 27/14블럭) · 밀양 내이동 2차(「내이3지구 1BL 2롯트」705·2025-06-05)
- **미검출 16**: [경남 5] 용원하버시티에일린의뜰 · 사천송지엘크루 · 거제장승포서희스타힐스 · 다인로얄팰리스부산신항2차(검색 0건) · 통영한림풀에버 / [울산 6] 남구 B-07(구역명 0건) · 문수로 비스타 더파크(원문 부재 = 인허가 전 실물 1호) · 달동더리브 · 대상웰라움달동(달동 후보 0건) · 화정1지구 서한이다음 · 청량읍 덕하리 / [부산 5] EDC 29BL(11~27BL 은 잡히나 29BL 없음) · 남부민동주상복합 · 다대동구획지 · 명지2 B11·B12 예미지 / + 동문디이스트
- **스코프 밖 4**: 시민공원비스타동원2차 · 범천동1090-61 · 쌍용더플래티넘센텀 · 연천시장 (부산진·해운대 — 나머지 22곳 완주 후 대조)

**A-3 갭 워치 5지표 정의** — pre_announcement 수 / apt_permits WHERE matched_site_id IS NULL / confidence='conflicting' / 동일 법정동 내 이름 유사쌍 / 정비축 stage_updated_at < now()-180d.

## 부록 B — 이번 검증 확정 사실 (P0-3·P0-4 근거)

| 구역 | 확정 | 근거 |
|---|---|---|
| 명륜2 | 래미안 마크 더 스위트, 삼성물산, 504세대 | 시공사 선정 2021.6 보도 다수 + 2025.10.31 사업시행변경인가(499→504) |
| 가야1 | 더 다이너스티 가야, HDC 55%·대우 컨소 | 수주 공시 2022.11(1,943) + 특화설계 변경 1,901 예정 |
| 남천2-3 | 그랑자이 더 비치 | 레코드 자기식별(삼익비치) + 공지 일치 |
| 구서3 | DL이앤씨(2021.9 수주, 도급 959억) — **361 vs 415세대 충돌** | conflicting 큐 1호 |
| 울산 B-07 | HDC(2021.8 선정), 지하4~지상40층 10개동, 펫네임 미정, 세대수 1,295~1,391 산포, 인가 지연(2024.12 조합장 교체) | conflicting 큐 2호 |
