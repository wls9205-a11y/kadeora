> **퇴역 (2026-09-03 · G-4)** — 이 파일의 전 규칙은 `docs/RULES.md` 로 이관·재등재됐다. 여기는 «스텁 색인» 전용이다.
> ⛔ 신규 등재 금지 — 새 규칙은 `docs/RULES.md` 에만 올린다. 인용은 `RULES#N` 형식으로 쓴다.

# Architecture Rules — kadeora

본 문서는 코드 변경 시 반드시 지켜야 할 architecture-level 규칙 모음. 새 규칙은 추가만 하고 삭제/번호 재할당 금지 (PR/세션 노트와 cross-reference 됨).

## Rule #11 — (이관됨)

→ `RULES#11` 로 이관(2026-09-03 G-4 원장 통합). 문안은 `docs/RULES.md` 가 정본이다.
⛔ 여기에 다시 적지 않는다 — 두 원장에 같은 규칙이 있으면 오늘 잡은 대역 충돌이 되돌아온다.

## Rule #17 — (재등재됨)

→ `RULES#117` 로 재등재(2026-09-03 G-4). 이 번호는 `RULES#17`(다른 규칙)와 충돌해서 비운다.
⛔ #17 를 다시 쓰지 않는다.

## Rule #116 — (재등재됨)

→ `RULES#143` 로 재등재(2026-09-03 G-4 퇴역 이전). 문안·의미 변경 없음.
⛔ 이 번호를 다시 쓰지 않는다.

## Rule #115 — (재등재됨)

→ `RULES#142` 로 재등재(2026-09-03 G-4 퇴역 이전). 문안·의미 변경 없음.
⛔ 이 번호를 다시 쓰지 않는다.

## Rule #18 — (이관됨)

→ `RULES#18` 로 이관(2026-09-03 G-4 원장 통합). 문안은 `docs/RULES.md` 가 정본이다.
⛔ 여기에 다시 적지 않는다 — 두 원장에 같은 규칙이 있으면 오늘 잡은 대역 충돌이 되돌아온다.

## Rule #19 — (이관됨)

→ `RULES#19` 로 이관(2026-09-03 G-4 원장 통합). 문안은 `docs/RULES.md` 가 정본이다.
⛔ 여기에 다시 적지 않는다 — 두 원장에 같은 규칙이 있으면 오늘 잡은 대역 충돌이 되돌아온다.

## Rule #20 — (이관됨)

→ `RULES#20` 로 이관(2026-09-03 G-4 원장 통합). 문안은 `docs/RULES.md` 가 정본이다.
⛔ 여기에 다시 적지 않는다 — 두 원장에 같은 규칙이 있으면 오늘 잡은 대역 충돌이 되돌아온다.

## Rule #21 — (재등재됨)

→ `RULES#123` 로 재등재(2026-09-03 G-4 퇴역 이전). 문안·의미 변경 없음.
⛔ 이 번호를 다시 쓰지 않는다.

## Rule #22 — (재등재됨)

→ `RULES#124` 로 재등재(2026-09-03 G-4 퇴역 이전). 문안·의미 변경 없음.
⛔ 이 번호를 다시 쓰지 않는다.

## Rule #23 — (재등재됨)

→ `RULES#125` 로 재등재(2026-09-03 G-4 퇴역 이전). 문안·의미 변경 없음.
⛔ 이 번호를 다시 쓰지 않는다.

## Rule #24 — (재등재됨)

→ `RULES#126` 로 재등재(2026-09-03 G-4 퇴역 이전). 문안·의미 변경 없음.
⛔ 이 번호를 다시 쓰지 않는다.

## Rule #25 — (재등재됨)

→ `RULES#127` 로 재등재(2026-09-03 G-4 퇴역 이전). 문안·의미 변경 없음.
⛔ 이 번호를 다시 쓰지 않는다.

## Rule #27 — (재등재됨)

→ `RULES#128` 로 재등재(2026-09-03 G-4 퇴역 이전). 문안·의미 변경 없음.
⛔ 이 번호를 다시 쓰지 않는다.

## Rule #28 — (재등재됨)

→ `RULES#129` 로 재등재(2026-09-03 G-4 퇴역 이전). 문안·의미 변경 없음.
⛔ 이 번호를 다시 쓰지 않는다.

## Rule #30 — (재등재됨)

→ `RULES#130` 로 재등재(2026-09-03 G-4 퇴역 이전). 문안·의미 변경 없음.
⛔ 이 번호를 다시 쓰지 않는다.

## Rule #31 — (재등재됨)

→ `RULES#131` 로 재등재(2026-09-03 G-4 퇴역 이전). 문안·의미 변경 없음.
⛔ 이 번호를 다시 쓰지 않는다.

## Rule #32 — (재등재됨)

→ `RULES#132` 로 재등재(2026-09-03 G-4 퇴역 이전). 문안·의미 변경 없음.
⛔ 이 번호를 다시 쓰지 않는다.

## Rule #33 — (재등재됨)

→ `RULES#133` 로 재등재(2026-09-03 G-4 퇴역 이전). 문안·의미 변경 없음.
⛔ 이 번호를 다시 쓰지 않는다.

## Rule #34 — (재등재됨)

→ `RULES#134` 로 재등재(2026-09-03 G-4 퇴역 이전). 문안·의미 변경 없음.
⛔ 이 번호를 다시 쓰지 않는다.

## Rule #36 — (재등재됨)

→ `RULES#135` 로 재등재(2026-09-03 G-4 퇴역 이전). 문안·의미 변경 없음.
⛔ 이 번호를 다시 쓰지 않는다.

## Rule #37 — (재등재됨)

→ `RULES#136` 로 재등재(2026-09-03 G-4 퇴역 이전). 문안·의미 변경 없음.
⛔ 이 번호를 다시 쓰지 않는다.

## Rule #38 — (재등재됨)

→ `RULES#137` 로 재등재(2026-09-03 G-4 퇴역 이전). 문안·의미 변경 없음.
⛔ 이 번호를 다시 쓰지 않는다.

## Rule #39 — (재등재됨)

→ `RULES#138` 로 재등재(2026-09-03 G-4 퇴역 이전). 문안·의미 변경 없음.
⛔ 이 번호를 다시 쓰지 않는다.

## Rule #40 — (재등재됨)

→ `RULES#139` 로 재등재(2026-09-03 G-4 퇴역 이전). 문안·의미 변경 없음.
⛔ 이 번호를 다시 쓰지 않는다.

## Rule #41 — (재등재됨)

→ `RULES#140` 로 재등재(2026-09-03 G-4 퇴역 이전). 문안·의미 변경 없음.
⛔ 이 번호를 다시 쓰지 않는다.

## Rule #42 — (재등재됨)

→ `RULES#122` 로 재등재(2026-09-03 G-4 정정). #21~#42 는 `docs/RULES.md` 가 아카이브로 비운 대역이라 여기서 재사용하면 언젠가 같은 번호가 두 규칙을 가리킨다.
⛔ #42 를 다시 쓰지 않는다.

## Rule #43 — (재등재됨)

→ `RULES#118` 로 재등재(2026-09-03 G-4). 이 번호는 `RULES#43`(다른 규칙)와 충돌해서 비운다.
⛔ #43 를 다시 쓰지 않는다.

## Rule #107 — (재등재됨)

→ `RULES#141` 로 재등재(2026-09-03 G-4 퇴역 이전). 문안·의미 변경 없음.
⛔ 이 번호를 다시 쓰지 않는다.

