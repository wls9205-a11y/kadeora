# 지시서_M4_20260903 — 메인 4화면 컴팩트 리뉴얼 (v2 확정)

대상 화면: 홈(/) · /apt · /stock · /blog
기준 문서: 설계안_M4_20260903(§ 번호 인용은 그 문서) + `docs/디자인시안_M4_20260903.html`
관계: 최종통합지시서 v2(야간판)의 부속. 충돌 시 마스터 우선.

---

## §0 판정 종결 (2026-09-03)

Node "좋아 이렇게 진행해보자" — v2 시안 + 추천안 일괄 승인으로 해석해 종결한다(「너 추천대로」 관례). 전 항목 Node 한 줄 지시로 뒤집기 가능.

- **Q1ⓐ** /apt 공고 전: 이미지 카드 그리드 → **행 + 40px 미니 썸네일**
- **Q2ⓐ** /stock 게이트 카드 **제거** (진짜 차단 아님 — 컴포넌트 호출 제거)
- **Q3 가·가** 홈 신설 2건: 「공고 전」 행 5 + 「분석 최신·부동산」 행 3 (V4-5 "신설 안 함" 판정을 이 승인으로 개정)
- **Q4ⓐ** /blog 피처드 = **부동산 카테고리 최신 우선** 선정 규칙 (기본 탭·색인 URL 불변)
- **Q5ⓐ** 홈 하단 링크 그리드 **제거** (푸터 네비 중복)
- **Q6ⓐ** /blog 인라인 검색 **보류** (마스트 검색 유지)
- **Q7 가** 「레일-본문 상호배타」 DS_RULES 등재
- **추가 확정(9/3 v2 피드백)**: /blog 「현장별로 모아 보기」 **제거** · 섹션 사이 **8px 밴드** · 섹션 제목 **골드 룰 기본**(kd-sechead rule opt-in) · 강조 위계 **제목 600 / 핵심 수치 700**(800+ 금지 불변)

## §1 걸쇠 · 순서 · 이관

1. **걸쇠 A**: `docs/디자인시안_M4_20260903.html` 커밋(M4-C0). 파일 동봉 — 저장소에 없으면 착수 중단(V4-0 관례).
2. **걸쇠 B**: 본대(M4-1~4)는 **V4D-FN-TY2의 P0-A·FN 종결 후** 착수. M4-0(결함 수리)은 예외 — FN 세션과 워크트리·파일이 겹치면 그 뒤, 아니면 즉시.
3. **TY2 이관(명시 개정)**: V4D-FN-TY2 §4(TY2)는 본 지시서로 흡수한다 — 본문 400→500 상향 + `--fw-*` 4종 토큰화(토큰 신설 금지 조항 대상 → **보고 의무**) + 800+ 잔여 59건 회수를 **각 화면 커밋에 동승**. 원 지시서 §4는 사문 처리.
4. 롤백 태그 `pre-m4-20260903`. 두 세션 병행 관례 준수: 착수 전 `git pull --rebase`, 새 Rule 번호는 origin 확인 후.

## §2 관통 원칙 (V4 승계 + M4 증분)

- 해석 계층: **Node 지시 > 본 지시서 > 저장소 기확정 표준 > 시안 시각 언어 > 시안 데이터(예시=무효)**.
- 시안·실물이 갈리면: 데이터 구조·열 구성·존재하는 값은 **실물 정본**, 시각 언어는 시안 정본. 갈림은 판정 요청 없이 실물 정본 처리 + 보고(V4-4 총괄 승계).
- h1 sr-only 색인 문자열 불가침. 서브마스트 타이틀 = 시각 라벨(heading 아님).

## §3 공통 스펙 (전 화면 커밋에 적용)

- **밴드**: 섹션 = 흰 블록, 섹션 사이 8px 풀블리드 밴드. 시안 #EAEEF4는 근사치 — globals.css 의 페이지 배경 계열 **실토큰 매핑 후 사용**(지시서에 토큰명을 못 박지 않는 이유: 실정의 미확인 상태 표기 금지 규칙). 매핑 불가 시 시스템 계층 신설 + 보고.
- **섹션 헤더**: eyebrow(모노 기확정 스펙) + 제목 600 + **골드 룰 22×3px** — kd-sechead rule opt-in 을 4화면 섹션 기본으로. 골드가 유일한 구획 신호 금지(항상 텍스트 동반, 기확정).
- **위계**: 행 제목·단지명·종목명 600 / 핵심 수치(가격·세대수·현재가·D-day) 700 / 보조 메타 한 톤 하강 / 본문 400→500(TY2). fontWeight 800+ 0건 유지.
- **행**: 인터랙티브 ≥ `--touch-min`, 비인터랙티브 정보 행 40px, `tabular-nums` 전역(수치 우측 정렬).
- **조건부는 조건부**: 댓글 0 → 미표기, 카운트 0 → 라벨째 미렌더(0곳 노출 금지).
- **이모지 0**(satori 썸네일 예외 유지) — /apt 바로가기 7종·🔔, /blog 📬📚🔗●💬, 홈 📍 전량.
- **더보기**: 목록 바닥 문장형 SectionLink 일원화, 「전체 보기↗」·헤더 화살표 수렴.
- 금지 재확인: view_count 노출 · 「인기」 라벨 · sticky/fixed+backdrop-filter · 전화 CTA · 검색바 재구현(hero variant 스타일만) · 카드 = 큐레이션 캐러셀 유일.

## §4 커밋 분할

**M4-C0 — 시안 커밋** (걸쇠 A)

**M4-0 — /apt 서브마스트 결함 수리** (판정 무관 선행 가능)
- `regionSiteCount` 전국 진입 시 0 원인 실측 → 수리. 주석의 「get_apt_region_counts 시도 소계 = 뒤로가기·전체 칩과 한 조회」 계약 유지 — 전국일 때만 다른 합을 쓰는 분기 금지, 조회가 전국 소계를 반환하게.
- 서브마스트 보조 줄 = 실동작과 한 소리: 「전국 N곳 · 상태 → 마감 임박 순」. 「모집공고 기준 최신순」 문구는 실정렬이 그것일 때만.

**M4-1 — /stock** (본대 1호, 최저 리스크)
- IssueGateCard 호출 2곳(L360·L489) 제거, 컴포넌트 파일은 타 사용처 grep 0이면 동반 삭제(삭제 함정 3형태 주의 — 접두 충돌·라벨 개명 생존·선택자 공유).
- 행 정규화: 태그 「등락폭·신선도」 → 최상위 사유 1개만 보조 메타에 인라인, 💬는 댓글 ≥1만.
- 시총 뷰에서 `*_IDX` 심볼 제외(지수는 종목 아님). 지수 수집 크론 신설 금지 불변.
- 뷰 미리보기 각 5행 + SectionLink. 밴드·골드 룰·위계·TY2 동승.

**M4-2 — /blog**
- 「현장별로 모아 보기」 렌더 제거(데이터·라우트 불변 — 상세의 관련 분석 링크는 유지).
- 목록 무절단: 세부 분류·태그 칩은 탭 직하 1곳, 가입 배너·시리즈·서비스 칩은 목록 종단, 「다음 페이지 미리보기」 블록 삭제(L952 부근).
- 피처드 선정: `category=realestate` 최신 1건 우선, 없으면 전체 최신 폴백. 색인 URL·기본 뷰 불변.
- 행 표준(카테고리 600 컬러 라벨 + 제목 + 날짜), 썸네일 상위 2건+빈 span 유지(V4-3). 「읽는 시간」은 피처드에만. 시리즈·태그 본문판 제거(레일 전용, 모바일 종단 1회). 밴드·골드 룰·위계·TY2.

**M4-3 — /apt** (최대 수술)
- 큐레이션 캐러셀(「전국 지금 주목할 청약」) 하단 → 단계 스트립 다음으로 이동. 🔔 제거.
- **청약 단일화**: 상태 그룹 헤더(접수중·임박 → 발표대기 → 계약중) + 그룹 내 D-day순. 노출량 = 접수중·임박 전량 + 발표대기 전량 + 계약 5 + 「지난 공고 더보기」 문장형(기존 /apt/archive 링크). **타임라인·이번 주 결과·이번 주 움직인 현장 섹션 제거** — 조회·컴포넌트는 타 사용처 grep 후 처리(홈의 「최근 움직인」은 존속 — 공유 컴포넌트면 렌더만 끊기).
- 공고 전: 카드 그리드 → 행 + 40px 썸네일(hero_image_url 체인 재사용, 없으면 브랜드 타일).
- 단계 스트립 → 4분할 스탯 셀(수치 700 + 라벨).
- 본문 「관련 청약 분석」·「바로가기 7종」 제거 — 레일 ③④ 일원화. 모바일 대체 = 목록 종단 축약 1회(관련 분석 3행 + 텍스트 링크 1줄).
- 검증 프로브: 성남복정2류 동일 단지 화면 내 노출 ≤2(캐러셀+목록). 밴드·골드 룰·위계·TY2.

**M4-4 — 홈** (마지막 관례)
- 지표 통합: KPI 듀오 + 실거래 위젯 → 3분할 스탯 1행(실거래·공고 전·접수중) + 표준 각주. 같은 숫자 이중 렌더 0.
- 신설 ①「공고 전 · 카더라 단독」 행 5(단계 변경 최신순, **기존 pipeline 조회 재사용 — 새 조회 금지**, 골드 배지 「카더라 단독」은 골드 bg+다크 텍스트) ②「분석 최신 · 부동산」 행 3(blog realestate 최신).
- 최근 움직인 4→7행, 재개발 3→5행. 지역 칩 불변(색인 자산). 하단 링크 그리드 제거. 검색 스테이지: hero variant 시각만 강화(높이·포커스·placeholder), 재구현 금지·hotkey false 유지. 홈 증시·리드폼 없음 불변. 밴드·골드 룰·위계·TY2.

**M4-5 — DS_RULES 등재** (본대 종료 후 소커밋)
- 신설 조항: ①레일-본문 상호배타(같은 정보 동시 렌더 금지, 모바일 대체는 종단 축약 1회) ②섹션 밴드 8px + 골드 룰 섹션 기본 ③카피=동작(실차단 없는 게이트 문구 금지) ④조건부는 조건부(0값 미표기).

## §5 착수 점검 · 게이트 (커밋마다)

- 착수 3항: 렌더 분기 수 확인 / chrome-stack-audit ROUTES 에 4화면 포함 확인 / 시안↔실물 대조표.
- `npm run build` + `npx tsc --noEmit` 둘 다(#68). 라우트 이동·삭제 시 `rm -rf .next/types`.
- token-snapshot: `--fw-*` 신설분(보고 동반) 외 빈 diff 목표.
- 배포본 grep 0 검증: 게이트 카피(「미리보기 끝」) · 「현장별로 모아 보기」 · 「다음 페이지 미리보기」 · 이모지 목록 · `*_IDX`(시총 응답 내).
- 수치 3종: /apt 섹션 10+→6 · 동일 단지 노출 ≤2 · 프로덕션 HTML 텍스트 바이트 전/후 실측 기록.
- 조건부 렌더는 렌더 조건 URL 로 검증(V4-5 산출 자 승계). 게이트는 초록으로 태어난다 — 신설 검증은 현행 화면 초록 확인 후 편입.

## §6 Node 몫 · 관찰

- M4-1 배포 후 실기기 눈검사 = **중단점 G**(D·E·F 형식). 통과 후 화면별 순차 진행.
- 계측 기준선: FN 으로 conversion_events 가 살아나면 M4-4 배포 전 7일 검색·클릭 기준선 확보 권장(효과 측정 전제).
- 관찰 항목: 골드 룰 남발 인상 여부(섹션 수 준 화면에서), 밴드 톤이 라이트 배경과 뭉개지는지, 700 수치가 S4-3식 오버플로를 만드는지(배지·pill 육안).
