import SignupFlowClient from './SignupFlowClient';

export const dynamic = 'force-dynamic';
export const metadata = { title: '가입 플로우 진단 — 어드민' };

export default function AdminSignupFlowPage() {
  return <SignupFlowClient />;
}
