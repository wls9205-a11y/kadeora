// H4-4 → H5-2 — `/apt` 의 뷰 전환.
//
// ⛔ H5-2(2026-08-27): «지도 항목을 내렸다».
//    부울경 정비사업 179곳 중 169곳에 주소·동이 없다(T 트랙 실측). 좌표가 없는 현장은
//    지도에 «아예 안 나타난다» — 지도를 전면에 두면 데이터가 없는 것이 아니라
//    「그 지역엔 현장이 없다」로 읽힌다. 없는 것과 못 그리는 것은 다르다.
//    지도는 `/apt/[id]` 위치 블록에서만 쓴다. 거기서는 좌표 있는 현장만 그린다.
//
// ⚠️ `/apt/map` 라우트와 MapClient 는 «지우지 않는다». noindex 인 채로 200 을 유지한다 —
//    색인 규칙 이력이 그 URL 에 붙어 있고, 지우면 그 이력이 404 가 된다.
//
// ⚠️ children 은 그대로 통과시킨다. 토글이 하나만 남았지만 컴포넌트를 없애지 않은 이유는,
//    되살릴 때 페이지 구조를 다시 건드리지 않기 위해서다. 지금은 껍데기다.

export default function AptViewSwitch({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
